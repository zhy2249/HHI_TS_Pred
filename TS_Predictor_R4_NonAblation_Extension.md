# R4非消融扩展：空间条件风险、因果规则选择、带符号平面预测

实施更新（2026-09-20）：六组现已接入codec/宏，见 [实现与使用](TS_Predictor_R4_Implementation.md)。
当前验证状态见 `TS_Predictor_R4_Validation.md`；公式不变，文末单列设计阶段历史。

版本：R4-20260920-v2。用户在原R4设计之后明确要求增加非消融实验。
**原R4-1/2/3公式、编号不变；新增R4-4/5/6。六组现已接入codec/宏/batch，尚无正式CTC结果。**
R3后续测试照常进行，不改变原程序、正在运行的任务、已有结果或先前结论。

主数据依据：`TS_Predictor_R3_LB_CE_Results_20260920.md`、`TS_Predictor_Cross_Round_Analysis_20260919.md`。
原R4：`TS_Predictor_R4_Experiment_Design.md`。正式基线一直是Current，R3-1只作增量对照和部分新规则的默认预测器。

## 1. 从数据出发，而不是从公式数量出发

### 1.1 不同方法确有内容互补，但尚无局部可识别性证据

| 序列 | 固定NoPred | 固定directional | R3-1 |
|---|---:|---:|---:|
| BasketballDrill | −0.16651% | **−0.25610%** | −0.12191% |
| RaceHorsesC | −0.02909% | **−0.04346%** | +0.00135% |
| FourPeople | +0.11888% | +0.20599% | **−0.08251%** |
| Johnny | −0.00628% | −0.11078% | **−0.13868%** |
| KristenAndSara | **−0.02869%** | +0.12295% | +0.13941% |

这些完整实测CE曲线支持“几种规则各有强项”，不支持直接全局换成directional或NoPred。
**序列级互补不能证明CG或系数级可以识别赢家**；因此新方案必须自己闭环编码，不能按序列拼接最佳曲线。

### 1.2 R3保护有效，但它仍把邻居当作同一种幅值分布

R3-1整体−0.09329%，明显优于R2-R的−0.03369%，不应丢弃这个有效起点。
然而实际代码把L/U/D/LL/UU的非零幅值放入无位置标签的集合，统一计算候选cost。
它没有区分“沿当前延续方向的样本”与“跨方向的样本”。原directional虽全局较弱，却在部分内容更好，
因此值得测试：**保留风险选择，同时引入空间方向信息**，而不是在两个算法之间硬切换。

这只是可检验的结构性假设；现有表格没有证明退化区域一定具有某种方向。

### 1.3 改善评分样本的使用方式，比继续增强历史门槛更有依据

R3-1在BQMall高/中/低质量区间为−0.14127/+0.09206/+0.18275%；
KristenAndSara为−0.04707/+0.23378/+0.23532%。剩余问题并非一个全局固定偏差。

R3仍然使用一组邻居的幅值选候选，又在同一批幅值上评价该常数候选。
删除最大正贡献只能减轻单个样本主导，不会把这个评分变成真正的预测回看。
新方向是：对已知位置j，先只用j之前的邻居算预测，再用q_j评价该规则；
由这些真实因果回看选择当前位置的规则，而不是把一个当前位置候选p套用到所有历史幅值上。

R3-3/4的高质量收益只保留父方法约42%/46%，去Party后退化，说明不应只把旧CG score再加大或加强门槛。
本轮不新增跨TU记忆或更大的全局EWMA，而采用短空间邻域、无持久状态的规则回看。

### 1.4 符号信息是尚未测试的特征，不是已发现的根因

此前所有新幅值公式及R3主要使用绝对值。带符号的q也是decoder已知信息，却没有进入这些幅值预测公式。
这提供一个新的模型方向；**RD表不含符号分布，不能声称某条序列就是因忽略符号而失败**。
旧幅值gradient的CE约+0.08462%，因此新的带符号方案不做无条件启用，且优先级低于前两项。

## 2. 新增三组与优先级

| 新编号 | 拟议名称 | 新机制 | 对R3主要改变 | 优先级 |
|---|---|---|---|---|
| R4-4 / DR | r4_directional_risk | 方向条件下的加权幅值风险 | 不再等权混合横/纵邻居 | 首批 |
| R4-5 / CB | r4_causal_models | 对预测规则作局部因果回看 | 从“选常数幅值”提升到“选局部规则” | 首批，关注开销 |
| R4-6 / SP | r4_signed_plane | 带符号平面幅值＋因果验证 | 引入符号关系及不在原幅值集合中的预测 | 探索性、次优先 |

它们都不是屏蔽原分支的消融：DR会改变样本相关性和候选排序；CB选择整条预测规则；SP生成新的预测幅值。
三组彼此独立，**不同时叠加**DR、CB、SP或原R4-3的补查，以免无法解释是哪套完整方法起作用。
所有组YUV共同启用，不再复制Y-only、QP阈值、不同K或权重比例扫描版本。
三组都是逐系数局部自适应；不宣称回答了“仅凭之前CG选择当前整CG mode”的问题。

## 3. 共用语义与边界

- f(a,p)、C_TS、Rice length和dynamic range沿用R3源码，不改remapping/符号语法、不使用实际CABAC context决定规则。
- C_TS是语法代理成本，不是真实CABAC bits；真实效果看完整闭环BD-rate。
- n>=3及“总优势减去最大单样本正贡献严格>0”仍为固定保护原则，不扫描阈值。
- p=0/1是identity等价类；平局规则固定，不能依赖容器迭代顺序。
- 只使用当前component TU内已可解码的q；无原像素、原残差、失真、lambda、搜索状态、未来CG/TU/frame。
- 各次多遍调用重算同一个纯函数，不维护会因Writer/Reader调用次数不同而变化的状态。
- BDPCM、native bypass/cutoff=0、非TS、TSRC关闭路径保持原行为。
- 不新增bitstream模式信令；未来实现后Encoder/Decoder必须选相同实验版本。

令R(z)表示**原R3-1算法**在位置z给出的幅值。所有后续对R的调用都调用这个固定原函数，
而不是递归调用正在设计的R4选择器。

## 4. R4-4：方向条件风险（DR）

### 4.1 方向来自已知邻域，不直接等于最终predictor

沿用现有directional的方向一致性式：

\[
e_h=|L-LL|+|U-D|,\qquad e_v=|U-UU|+|L-D|.
\]

这里全部为幅值。完整几何模板不存在（x<2或y<2）时，直接使用R(z)，保持R3边界覆盖。
方向不是选择left/above的硬开关，而是给风险评估的相关样本增加固定权重：

\[
w_L=w_{LL}=1+[e_h<e_v],\quad
w_U=w_{UU}=1+[e_v<e_h],\quad w_D=1.
\]

权重只有1/2；相等时全为1。**2:1只是预先固定的空间相关性假设，不是从结果拟合出的最优比例。**
实际零邻居参与几何一致性计算，但与R3相同，不计入非零评分样本；非零样本不足3则Current。

### 4.2 风险选择与保护

候选仍为原集合P={0,p_C}∪非零邻居幅值，p_C=max(L,U)，无额外幅值公式。

\[
d_i(p)=C_{TS}(f(a_i,p_C))-C_{TS}(f(a_i,p)),
\]
\[
G_w(p)=\sum_i w_i d_i(p),\quad
B_w(p)=\max(0,\max_i w_i d_i(p)),\quad H_w=G_w-B_w.
\]

先选G_w最大者，平局仍Current→identity→较小幅值；只有该winner的H_w>0才采用，否则Current。
**移除的是一整个位置的加权贡献w_i d_i，不是只减d_i，也不把权重2当作两个独立样本。**
不加原R4-3补查。e_h=e_v时必须精确退化为原R3预测，可作单元断言。

### 4.3 可检验预测与风险

希望在方向较一致的局部区域减少另一方向样本的干扰，同时保留NoPred和多幅值的成本竞争。
它不是固定directional复测，也不是为了某条序列定方向。

风险：方向式本身可能错误；量化/稀疏性可能使e_h/e_v不稳定；权重和候选都从同一批样本得到，仍可能过拟合。
若有大量方向条件改动，却不优于R3或反而扩大中低质量损失，则否定这一版，而不是继续扫描比例。
额外运算只是在现有五样本上计算方向和乘1/2，复杂度最接近R3。

## 5. R4-5：因果局部规则选择（CB）

### 5.1 比较预测“函数”，而非把同一个候选p套到所有历史值

四个固定专家：

\[
P_R(z)=R(z),\quad P_C(z)=\max(L_z,U_z),\quad P_N(z)=0,\quad P_D(z)=\text{原directional}(z).
\]

directional沿用实际代码的几何边界及tie-break，不引入新权重或signed plane。
对当前位置i，选取V_i={左、上、左上、左二、上二}中有效、q_j非零的历史目标位置，n=|V_i|。
不足3个目标位置，默认R(i)，**不是默认Current**，以保留R3在冷启动/小TU的已有能力。

对于每个j∈V_i：

1. 用j自身之前的因果邻域分别计算P_R(j)、P_C(j)、P_N(j)、P_D(j)。
2. 计算预测时，**任何专家都不能读取q_j本身**；读到q_j来挑候选再回头奖励命中，会破坏这组的定义。
3. 预测算完后，才把已知a_j=|q_j|作为评价目标。

这需要读到更远但仍因果的邻居，最远可到横/纵四格；不存在位置使用各原专家规定的边界行为。
不跨TU，不把不存在的邻居当观测样本。实际scan下的因果性须在未来codec单元测试中验证。

### 5.2 评分与输出

评分默认方法为R3：

\[
d_j^{(m)}=C_{TS}(f(a_j,P_R(j)))-C_{TS}(f(a_j,P_m(j))),
\]
\[
G_m=\sum_{j\in V_i}d_j^{(m)},\qquad H_m=G_m-\max(0,\max_jd_j^{(m)}).
\]

先选最大G的规则，平局顺序 **R3→Current→NoPred→directional**；若winner的H>0，输出P_m(i)，否则R(i)。
不叠加R4-3补查，也不把先前选中的规则递归用于回看。

这里R3只是选择器内部的比较对象；**正式BD-rate anchor仍是Current**，不能把R3当成新的正式baseline表。

### 5.3 与旧CG历史选择的区别

- 每个位置根据附近历史目标重新选择，不让整CG共用一个过时mode。
- 评价的是每个专家在历史位置自己的实际预测，而不是一个常数候选对一组幅值的自拟合。
- 默认保留已有效的R3，不像NG从Current开始、再等待历史支持NoPred。
- 没有累积EWMA，也没有跨CG更新顺序问题；跨CG边界只按真实空间因果邻居自然访问。

这减少了直接把目标a_j用于生成预测p_j的“自命中”，但样本仍相关、winner仍由小样本选择，
**不是独立交叉验证，不保证统计无偏或泛化。** 原R3内部还有其自己的模板拟合，未被神奇消除。

### 5.4 评分语义必须固定

对V_i每个非零目标，都应用同一个C_TS损失与当前TU的不可变Rice/dynamic range。
**这不是回放j当时的真实语法，不按j原来是否进入regular/bypass而筛样本，也不是CABAC历史比特。**
否则需要另加语法历史状态，破坏当前纯函数定义和多遍同步。当前i是否实际采用remapping仍由原生语法条件控制。
此约定也适用于下节SP，不能在未来实现时悄悄改变。

实现中每个历史目标的R3输出和基准cost只计算一次再复用，至多约6次R3局部计算加若干简单专家，
开销高于DR。初版不缓存依赖搜索中可变q的结果；优化需单独证明缓存失效与回滚正确。

若CB不能优于R3，或只产生大量Current/NoPred切换但无稳定RD增量，则不继续扩专家数/窗口。

## 6. R4-6：带符号平面候选＋因果验证（SP）

### 6.1 真正不同的预测幅值

使用带符号最终量化邻居q_L、q_U、q_D，而不是先取绝对值：

\[
\hat q_S=\operatorname{clip}_{[\min(q_L,q_U),\max(q_L,q_U)]}(q_L+q_U-q_D),
\qquad P_S=|\hat q_S|.
\]

即带符号的clipped plane/median-edge形式。中间运算用64位；输出满足P_S≤max(|q_L|,|q_U|)，不无界外推。
L/U/D几何位置不齐时，P_S(z)=R(z)，不牺牲原R3边界能力。
**仅改变magnitude predictor，不改变当前系数的sign syntax或额外预测其符号。**

例如q_L=5、q_U=−1、q_D=1：

- 原幅值gradient给5；
- 新带符号plane给|5−1−1|=3。

3可能不在原邻居幅值集合中，因而不是原候选的开关或重排。
例子只说明候选不同，最终是否采用仍由下述因果回看决定。

### 6.2 不是无条件复测一个gradient

本组仅比较两个固定规则：R3与上述P_S。不混入DR或CB的完整专家池。
使用与CB相同的V_i，对每个历史目标j先用j之前的邻居预测，再用a_j评价：

\[
d_j=C_{TS}(f(a_j,R(j)))-C_{TS}(f(a_j,P_S(j))),\quad
G=\sum_j d_j,\quad H=G-\max(0,\max_jd_j).
\]

n>=3且H>0才用P_S(i)，否则R(i)。目标i的真实q_i绝不能用于决定这个开关。
历史目标j缺L/U/D时，P_S(j)=R(j)，其贡献为0；保留该有效非零目标在V_i中，不另调样本门槛。

旧幅值gradient的负面结果使SP具有明确失败风险：残差符号可能根本不具有可延续的平面结构。
新增方案利用不同信息并要求先前实际预测有支持，仍不保证超过旧gradient或R3。

### 6.3 需要什么证据才继续

除RD外，必须看到实际候选与原幅值gradient/R3不同、通过验证、并改变目标remapping的覆盖。
区分“仅输出identity”和“产生新非identity幅值”，以及同号/异号/含零邻域。
若看不到独有预测作用，不因名字带signed就宣称利用了符号结构。
若有活动却仍主要扩大中低质量损失，否定此版，不追加更多高阶梯度组合。
与CB类似，回看R3的额外开销需要实测；暂列第二优先级。

### 6.4 Decoder已知符号不等于当前coeff数组带符号

源码 `CABACReader::residual_coding_subblockTS()` 的第一遍读sign，保存在signPattern/sigBlkPos；
第二/第三遍对幅值运算，第三遍逆remapping结束后，才在“set final coefficients”循环恢复整个CG的符号。
因此在第三遍调用SP时，**直接对coeff邻居取正负号会与Writer/RDOQ不一致**。

未来实现必须提供只读signed-neighbor视图：前面已完成CG读取最终signed coeff；
当前CG中已经完成逆映射的位置，用已恢复的幅值与已解码符号组合。只访问当前scan之前的位置，
不使用当前或未来系数；回看专家使用同一视图。符号在当前位置之前已从码流得到，不新增sign语法。
Writer读取最终signed q；RDOQ读取前面已确定、已赋符号的暂存q，CG清零后后续自然看到0。
不得为方便预测提前改写原pass1/2系数数组或污染context；也不把Encoder原始残差符号作为选择输入。

本轮原型输入是最终signed网格，没有模拟这个Reader暂存状态，因此不能据原型通过宣称编解码已同步。
若无法验证所有邻居在实际scan/多遍下的可用性，SP不得进入正式编码实验。

## 7. 三组各自检验什么，不作混合归因

| 方案 | 新增的统计假设 | 最有意义的反证 |
|---|---|---|
| DR | 几何方向可提高邻居幅值分布对当前目标的相关性 | 改了方向风险排序，但跨内容/区间无稳定增量 |
| CB | 附近位置上的因果预测成绩可用于选择当前局部规则 | 回看分数高，却仍错误切换或不如R3 |
| SP | 带符号关系可产生绝对值模型缺少的有效幅值 | 有独有幅值活动，仍无RD收益或开销过高 |

三组都是完整新政策，不是严格单因素因果识别。比如SP若成功，还不能单凭此组断言全部增量都来自符号，
而非回看保护的交互；之后是否补对应控制，应由证据决定，不现在展开所有组合。
它们也不能把R3某条序列的损失“修复”视为普适成功：必须保留完整CE及后续B/RA。

## 8. 实验规模、顺序与比较

### 运行优先级

1. 保持R3后续验证，不修改或替换正在跑的程序。
2. 原R4-1/2/3保留，84项CE设计不变。
3. 新增优先DR、CB，各28项，共 **56项**；SP是完整定义的探索性组，再追加28项。
4. 六组若全部执行，CE共 **168项**（比原方案增加84），不是默认全部立刻启动。
5. 当前已实现六组宏和batch模式，共享池不设组间屏障、不输出重建、每组完成即写表；正式任务尚未启动。

LB仍为C4+E3、半帧、QP22/27/32/37。Current Reference不变，不重跑整套anchor；
R3-1旧曲线只有在版本/配置/帧数可比性确认后用于增量比较。新组不补缺点。

### 评价与扩展门槛

沿用原R4：对Current主BD、直接对R3-1重新积分、分量BD后6:1:1、七/十二序列等权；
完整序列/分量/质量区间、共同三方质量范围比较、leave-one-out、bootstrap、cubic敏感性和复杂度。

新增组要进入后续测试，至少：

- 对Current CE平均<0、中位数<=0、去Party后<=0；
- 直接对R3-1 CE平均<0、配对中位数<=0；低质量共同区间不劣于R3-1，高质量仍有收益；
- 有该组独有的实际remapping活动、可核对远端身份和可接受Decoder开销；不以微小平均差掩盖广泛退化。

仍最多选 **一个R4候选**（六组共同竞争，而非每个子批次各选一个）扩展。
按完整CE直接对R3-1的均值排序；精确平局按实测Decoder开销、再较小编号，CI跨0保留不确定性。
默认后续RA CD32项、通过后LB B20项；全计划上限168+52=220个新增正式任务，不含短帧。
实际执行需届时结合R3结果和资源再决定，不因为新方案多了就扩大长跑。

CE已经多次用于设计，不能再当独立验证集；也不能宣称已观察的B/RA是全新未见内容。
工程目标仍需真实BCE达到−0.05/−0.08/−0.10%，不从CE替代推断。

## 9. 正确性与机制统计要求

未来实现需通过原R4的全部bit-exact/编解码/scan/矩形与TS尺寸验证，并新增：

- DR：e_h=e_v与R3一致；边界R3 fallback；删除完整加权位置而非半个贡献。
- CB/SP：预测历史目标j时污染q_j不得改变任何primitive专家预测；污染当前q_i及未来位置不得改变选择。
- 历史专家必须是固定R3/Current/NoPred/directional/SP，不递归调用CB/SP选择器；Rice参数不可因调用顺序变化。
- SP带符号运算范围、abs、边界、正负混合、p=0/1等价及保持原sign syntax；
  Reader当前CG的sign视图与最终signed网格一致，不直接信任第三遍中尚未恢复的数组符号。
- Writer/Reader多遍一致；RDOQ临时CG清零/回滚不留下缓存或跨TU状态。
- 新旧宏互斥，关闭或运行Current时bit-exact；与正在测试的R3二进制独立构建。

默认final Writer在线聚合，不把encoder临时搜索统计为最终样本，不输出大量逐系数文本。
保留实际component/W/H/CG数/位置、CU QP、intra-inter及原计数，并补：

- DR：横/纵/平局、非零支持、相对R3候选变化、H_w接受/拒绝、实际mapped改变。
- CB：有效回看样本数，各专家被选次数、G/H分布、模型变化但p等价/实际mapped改变的区分。
- SP：同号/异号/零邻域，signed与旧绝对值gradient是否不同，新非identity幅值是否出现在原模板外，
  回看通过/拒绝、实际mapped改变；预测符号不作为新增语法。

所有对最终q的目标误差/cost观察只能事后统计，不回流到当前选择。条件最终TS样本仍有选择偏差；
机制日志不能代替闭环RD，也不能据它宣称η或CG persistence已经测得。

## 10. 设计阶段原型的验证范围（历史记录）

以下记录实现之前的原型交付；当前六组宏/codec已实现，真实验证范围另见实现与验证文档。

- 本文冻结三组公式、tie-break、边界、评分语义、优先级和失败条件。
- 新增 `scripts/ts_r4_extension_design_check.cpp`，只是独立设计原型，不是codec实现。
- 复用真实R3/remapping/syntaxCost，在32000个人工网格目标上验证边界、保护条件、
  当前/未来污染不影响预测、primitive专家不读自身目标，以及signed输出范围；三组均存在实际映射差异。
- 人工检查中DR/CB/SP分别出现184/6569/7342次映射差异；**不能将这些数当视频覆盖率、预测准确率或收益**。
- 尚未验证真实CABAC多遍、RDOQ闭环、Decoder耗时或任何CTC效果，没有据人工计数筛选正收益方法。
- 未改codec、TypeDef.h、CMake、batch、Encoder/Decoder程序、原结果或正在运行的R3。
- 未来拟议R4宏范围扩为0..6，其中4/5/6对应本文件；当前宏和模式不存在，不能直接运行。
- 新结果收件目录现按宏值编号：`experiments/ts_predictor_r4/r4_<R4_MODE>_<方法名>/LB_CE/`，仅README，不生成假结果表。

原型检查命令（非编码实验）：

```bash
r4_ext_dir=$(mktemp -d /tmp/ts-r4-extension-XXXXXX)
g++ -O2 -std=c++17 -Isource/Lib/CommonLib scripts/ts_r4_extension_design_check.cpp \
  -o "$r4_ext_dir/ts_r4_extension_design_check"
"$r4_ext_dir/ts_r4_extension_design_check"
```

下一步应是用户确定优先实施哪些新组，再进行共享预测入口、宏互斥和短帧编解码验证，而不是立即启动长跑。
