# R6之后：最小化新增实验的计划

日期：2026-09-24。状态：**设计草案，尚未实现新模式、宏或启动编码**。
Current 是唯一正式 anchor，原 R3-1 是每项增量对照；不得将NoPred改为正式anchor。

## 1. 已有证据决定什么

- 七组R6都未通过2026-09-23预设的B准入条件；不直接扩大本轮B/RA，不组合2+5。
- 保留R3的Current fallback和guard；“天然命中/不对称”是真实结构，不是已经证实的RD缺陷。
- 停止sum-mincost、identity相对删贡献的继续参数扫描。
- 稀疏双L/U保留max，mean/min本轮更差；不扩充更多平均/中值候选。
- R6-5的Y增量−0.010822%，5/7好，中位数−0.027173%；UV却拖累，总增量+0.045027%。
  Y的CI跨0、高质量段+0.014915%，因此只支持一个Weak分量消融假设，不是新收益承诺。
- R3-1完整BCE−0.075521%；距离−0.08%仅0.004479个百分点。但不能用这一差额反向调规则追数值。

## 2. 优先实验 A：仅亮度的稀疏覆盖

逻辑标识 `Y-sparse-max`（设计名，不是当前程序可接受的CLI模式）。

设p_R3为原R3-1在相同因果邻域上的预测，n为L/U/D/LL/UU非零位置数。

\[
p=\begin{cases}
p_{R3}, & component\ne Y\text{ 或 }n\ge3,\\
\max(L,U), & component=Y,\ n=2,\ L>0,\ U>0,\\
0, & \text{其余Y的}n<3\text{位置}.
\end{cases}
\]

更紧凑的非等价覆盖条件为：Y、n为1或2、恰一个直接邻居非零且其幅值>1时改0；其它情况不改变原R3映射。
编码实现宜复用R6-5及分量分派，测试两种表达式的映射等价。
不是旧R3-2：**色度继续原R3，而非Current；Y的n≥3也完整保留原R3。**
不新增cost、不调guard阈值、不改Rice、不维护新历史、不新增syntax、不碰BDPCM/普通变换。
同一纯函数必须进入TS-RDOQ、Writer和Reader；不得只改最终Writer或做固定q评估。

解决的问题：稀疏新增规则是否在不撤掉原色度R3保护的情况下具有实际价值。
重要局限：Y/U/V BD共享整流码率，存在联合RD反馈；仅Y改动也可能改变色度指标。
因此不能将R6-5 Y与R3 UV离线拼接成此方法的预计BD-rate，也不能认为色度一定不变。

## 3. 可选成对诊断 B/C：不是立即扩大的参数矩阵

若需要分开解释“孤立样本”和“两个位置但缺少双直接邻居”，可在本轮A结果揭晓前决定同时测试：

| 设计名 | 相对原R3唯一的覆盖范围 | 其余区域 |
|---|---|---|
| B：Y-sparse-n1 | 仅Y，n=1，恰一个直接L/U非零、幅值>1时NoPred | 原R3 |
| C：Y-sparse-n2 | 仅Y，n=2，不是双直接L/U、Current>1时NoPred | 原R3 |

二者覆盖互斥，A的有效改动是B和C覆盖的并集，但**闭环BD增量不具有可加性**。
默认只优先A；B/C必须成对作为预先决定的机制消融，不在A失败后查看结果、挑更好的n值反复救活方向。
没有日志证明n1/n2具体损益，不能预言哪一支更好。先回收已有统计可以评估覆盖是否足以值得花预算，不能用conditional proxy预认证收益。
本阶段不再加入新候选幅值、相似度阈值、大小块白名单、序列例外或QP区间开关。

## 4. 活动与运行身份核验

先回收已有R6完整stdout+stderr，不要求重跑已经完成的正式实验。
核对每项真实policy/模式/版本、编解码对应关系、实际帧数、二进制/源码SHA、配置与hash。
统计至少分component、真实W/H、CU-QP、support n、CG位置：

- active_count与active_nonzero，明确是最终Writer regular访问口径；
- remap_vs_r3与remap_vs_r3_n1/n2，不能用p变化数冒充实际coding变化；
- lu_only/unequal/lu_remap_vs_r3，确认max/mean/min实际分离；
- proxy_vs_r3_positive/negative_abs，只用于诊断；
- R6-2另看dense_rejected与fallback_rejected，不混入no_proposal。

当前CSV/XLSM没有这些信息，不推断“收益来自某TU尺寸/某CG位置/高QP某分支”。
即使取得统计，它只覆盖最终TSRC样本，受工具选择条件影响；不能称无偏Oracle或独立验证。

## 5. 分阶段执行，冻结退出标准

### 阶段0：身份、正确性与活动，不评价BD

实现前先固定上述公式；沿用TypeDef.h主宏选择和新旧模式互斥，运行参数覆盖需明确banner。
继续复用当前批量共享任务池、每组结束即写表、resume、无重建设置；不额外造一套调度器。
结果目录遵循公开模式号前缀；待实际实现时分配编号，不提前创建看似已有结果的空白工作簿。

单元检查包括：Y/UV分支隔离、n=0/1/2/≥3、L/U等于1的映射等价、双直接邻居max、无当前/未来读入；
原生CABAC round-trip、统计开关不改码流、Current/OFF及旧模式回归。
固定快速集合BasketballDrill、BQMall、KristenAndSara，LB QP22/37各3帧；范围不足预先允许延长16帧。
需要实际相对R3 remap活动，不以短帧BD筛选；若持续无活动或不同步则暂停长跑，不改阈值制造活动。

### 阶段1：A的完整LB CE半帧，28任务

QP22/27/32/37，七序列都保留。已有Current不重跑；采用已核对的R3父结果。
如果必须重编父程序验证构建，仅做短帧身份/hash检查，不默认重跑整套anchor。
不得用anchor补缺点作正式准入判断；缺点只报告完成率、等待补齐。

下一轮预先固定以下门槛，不根据A结果修改。其中去KristenAndSara检查为本轮分析后新增的稳定性要求，
不追溯改写R6的原准入条件；R6已经未通过原来的均值/中位数必要条件。

1. CE对Current均值<0。
2. 直接对R3的CE均值<0，中位数≤0。
3. 去Party和去KristenAndSara各自的直接对R3均值均≤0，避免依赖单一强收益内容。
4. 三方共同低质量区间直接对R3的CE均值≤0；高质量区间对Current仍<0。
5. 运行身份、解码hash和有效活动通过，未见不可接受的复杂度开销。

置信区间/分位/逐序列同时报告；不强求七序列CI不跨0才准入，也不把微小负均值视为确定突破。
报告全部分量，主判据仍6:1:1，不因Y好就忽略UV。
若可选B/C已预先启动，也各自同样评价，不能合成每序列赢家的新曲线作为算法结果。

### 阶段2：仅通过门槛的一组扩展B，20任务

默认只有A可进入；若预先决定并完成B/C，仅一组按直接对R3增量、稳定性及复杂度选择，记录选择偏差。
CE是反复使用的开发集；B也已有历史观察，不宣称严格盲测，但新模式的B不能用于反复调规则。
完整BCE十二序列等权，仍分别报告B、C、E，避免CE收益掩盖B退化。
目标不变：−0.05%达标、−0.08%不错、−0.10%可观；A需证明对原R3有增量才值得替换。
若B翻转或新增幅度不稳定则保留R3，不再围绕B内容添加白名单。
RA兼容性/普适性测试放在此后，不同时铺开全部模式和配置。

## 6. 若A失败，下一步不是继续放宽NoPred

停止当前这一族稀疏覆盖规则，保存Negative结果与原R3实现。
若后续仍研究预测器，真正未解决的是“小样本中产生候选并用同一样本打分”及“整数cost与实际TSRC代价不匹配”，
不是已知要把Current的保护进一步削弱。先明确新的可复现评估信息、同步约束和实际活动，再提出独立假设。
本计划不提前把跨TU历史、复杂CABAC回放或新的预测公式当作下一轮必做项。
