# R3：有证据才偏离Current的TS predictor实验

版本：R3-20260919-v1。状态：**四组已实现并通过本地回归；2026-09-20已收到完整服务器LB CE结果。**
结果分析见 `TS_Predictor_R3_LB_CE_Results_20260920.md`；尚无R3 B/RA结果。下文冻结规则不作事后改动。
本文冻结算法与实验规则；运行说明见 `TS_Predictor_R3_Implementation.md`，
已验证范围与尚未覆盖事项见 `TS_Predictor_R3_Validation.md`。
依据：`TS_Predictor_Cross_Round_Analysis_20260919.md`。

## 1. 目标与边界

本轮研究：能否保留已有方法的有利区域，同时减少证据薄弱时偏离Current的损失。
不是继续增加很多幅值公式，也不假定“保护越严格越好”。

- 唯一正式anchor为Current，保留 `scripts/JVET-hhi.xlsm` Reference，不全套重跑anchor。
- 主指标仍为各分量独立PCHIP BD-rate后 `(6Y+U+V)/8`，负值好。
- CE七序列、BCE十二序列等权；BCE≤−0.05%达标、≤−0.08%不错、≤−0.10%可观。
- 全部是TS-RDOQ、CABACWriter、CABACReader共同生效的闭环工具实验，允许q、TS选择及后续决策改变。
- NoPred仍为候选和辅助对照，不替代Current，不作为正式收益或B准入的第二个anchor。
- 不使用当前待编码幅值、原始残差、失真、lambda、搜索信息或未来CG选择predictor。
- 不按序列名称、class、名义QP、分辨率或POC设开关；状态仅component TU内保持。
- 最初仅设计与建立收件目录；按后续“开始修改算法”请求现已实施，未启动正式CTC。

## 2. 从结果到假设：避免将相关性误作原因

| 已观察证据 | 本轮待检验假设 | 不能提前声称 |
|---|---|---|
| R2-N高质量区间−0.16225%、7/7改善，低质量+0.15200% | 对薄弱历史证据增加保护，可能减少低质量损失 | 高QP失败一定来自单个系数或稀疏性 |
| R2-R比多数规则好，但仍有明显序列退化 | 局部小样本的总成本优势不足，要求优势不由单一样本决定 | 模板cost最低就能预测当前系数 |
| R2-N/F在一些序列比Current和固定NoPred都差 | 不能仅因累积score仍为正，就继续离开Current | 所有失败都是旧score残留造成 |
| R2的U均值退化，N/F损失又集中于KristenAndSara | 单独改变作用分量，检验色度干预是否值得保留 | U是唯一根因，关闭色度一定改善 |
| R2-F尚无稳定增量支持额外复杂度 | 首轮不继续增加虚拟CABAC回放复杂度 | CABAC评分路线已被彻底否定 |

这里的保护假设尚无内部trace证实；本轮就是对其进行可证伪检验。
QP/质量区间只用于结果分层，不进入首轮模式选择。

## 3. 四组核心实验：两个机制、两种分量范围

| 新编号 | 模式名 | 方法 | 作用分量 | 直接对照 |
|---|---|---|---|---|
| R3-1 / RG | r3_risk_guard | R2-R局部winner增加删一贡献检验 | Y/U/V | 既有R2-R：检验局部保护 |
| R3-2 / RGY | r3_risk_guard_y | 与RG完全相同 | 仅Y；U/V用Current | RG：仅改变分量范围 |
| R3-3 / NG | r3_cn_guard | R2-N增加最近CG稳健证据条件 | Y/U/V | 既有R2-N：检验历史保护 |
| R3-4 / NGY | r3_cn_guard_y | 与NG完全相同 | 仅Y；U/V用Current | NG：仅改变分量范围 |

RG/RGY属于**局部因果预测**，允许读当前CG内先前解码邻居；不是纯历史CG选择器。
NG/NGY才是**仅利用之前完成CG选择当前整CG模式**的实验，原始研究问题由这两组回答。

R2-R和R2-N的完整CE表已有，可在可比性条件成立时复用，不重复正式编码。
R2-F缺点的补齐仍建议完成，但它不作为本轮主对照，不阻塞本轮设计。
不扩大gradient、modal或复杂fractional selector，不在本轮加入三候选历史混合。

**消融边界**：本矩阵能测“全分量下增加保护”和“已有保护下限制到Y”。
没有未保护的R2-R-Y/R2-N-Y新组，因此不能声称已分离保护与分量的全部交互效应。
若只Y组胜出而全分量保护组失败，下一步才补对应一个未保护Y组，查明保护是否必要；不直接认可两项改动都有效。

## 4. 共用的删一贡献检验

对已知历史样本i，定义候选相对Current的代理成本改善d_i，正数表示候选较便宜。
令

\[
G=\sum_i d_i,\qquad B=\max(0,\max_i d_i),\qquad H=G-B.
\]

只有H>0才通过。

含义：保持候选和参考固定，去掉最有利的一项贡献后，总优势仍为正。
在G>0时，这等价于删除任意一个样本后仍有正优势；它自动要求至少两个正贡献，且足以覆盖负贡献。
空集合、全零贡献、平局及H=0全部不通过。

例：

| d_i | G | B | H | 判断 |
|---|---:|---:|---:|---|
| [2] | 2 | 2 | 0 | 拒绝仅一个支持样本 |
| [2,2] | 4 | 2 | 2 | 通过 |
| [2,2,−2] | 2 | 2 | 0 | 正支持不足以稳健覆盖代价 |
| [2,2,2,−2] | 4 | 2 | 2 | 通过 |

这是预先固定的**删除一个贡献的敏感性规则**，不是统计置信区间，不假设样本独立，
不是删除样本后重新选predictor的交叉验证，也不能修复所有小样本偏差。
“删除1项”是本轮唯一固定稳健程度，不扫描删除0/1/2项后挑最优。
相关邻居重复同一幅值仍可能一起误导，因此不保证新方法更准确。

## 5. R3-1：局部成本winner的稳健验证

### 5.1 完全保留R2-R的候选、模板与成本

- p_C=max(L,U)，模板为因果可用的L/U/D/LL/UU，取实际非零值T+。
- 非零样本n<3则Current；不存在的邻居不当作样本。
- 候选为{0,p_C}与模板内幅值，p=0/1规范化为identity。
- 沿用 `TsFixedPrediction::localPredict` 的R2-R排序与Current优先tie-break，得到唯一p_R。
- 沿用 `syntaxCost`，包括Rice余数实际长度：

\[
C_{TS}(0)=0,\quad C_{TS}(1)=1,
\]
\[
C_{TS}(t\ge2)=2+\sum_{k=1}^4[t\ge2k]
 +[t\ge10]L_{Rice}((t-10)>>1,r,d).
\]

r与d仍为不可变的decoder-known TU参数；不使用当次多遍调用中不断改变的bin预算作为预测输入。
这仍是语法长度近似，不是真实CABAC rate。

### 5.2 新增的唯一决策条件

对模板内每个a_i，计算

\[
d_i=C_{TS}(f(a_i,p_C))-C_{TS}(f(a_i,p_R)),
\]

其中f为现有remapping，

\[
f(a,p)=\begin{cases}0&a=0\\1&a=p>0\\a+1&0<a<p\\a&a>p.\end{cases}
\]

最终：H>0用p_R，否则p_C。若p_R与p_C映射等价，直接Current。
不在失败后继续寻找第二候选，不更改n>=3，也不引入QP、幅值绝对阈值或分量专用权重。
因此同一输入下RG只可能采用原R2-R的winner或Current，不会产生第三种winner。

例子（r=1，模板前两项为L/U）：

| T+ | p_C | 原R2-R winner | d_i | H | RG预测 |
|---|---:|---:|---|---:|---:|
| [8,2,2,2,3] | 8 | 2 | [−5,2,2,2,1] | 0 | 8，回到Current |
| [8,2,2,2,2] | 8 | 2 | [−5,2,2,2,2] | 1 | 2 |
| [4,2,1,1,1] | 4 | 0 | [−3,0,2,2,2] | 1 | 0，NoPred |

这些例子只证明规则可接受/拒绝不同幅值候选；不证明真实编码收益。
模板可能横跨已完成CG和当前CG的因果部分；不引入额外跨CG状态，单CG TU仍可能生效。
额外复杂度为对已选winner最多5个样本累加/取max，保留已有候选搜索。

## 6. R3-3：最近CG稳健证据 + 原历史score

### 6.1 保持原R2-N评分，隔离保护条件的作用

不更换C_log、不加入fractional评分、不改变EWMA时间常数和初始状态。

\[
C_{log}(0)=0,\quad C_{log}(a>0)=1+2\lfloor\log_2a\rfloor.
\]

CG_j所有最终RDO决策完成后，在原R2-N使用的实际分支有效remapping位置E_j计算：

\[
d_{j,i}=C_{log}(f(|q_i|,p_{C,i}))-C_{log}(|q_i|),
\]
\[
G_j=\sum_{i\in E_j}d_{j,i},\qquad B_j=\max(0,\max_{i\in E_j}d_{j,i}),\qquad H_j=G_j-B_j.
\]

E_j必须复用 `finishTsPredictorCG` 的最终q三遍预算回放；不得换成RDOQ近似预算，
也不偷偷改成两个候选分别传播预算的完整CABAC仿真。

原state仍更新为：

\[
S_{j+1}=clip(S_j-trunc(S_j/4)+G_j,-32767,32767).
\]

所有整数行为与R2-N保持一致，包括|S|<4时衰减为0。

### 6.2 当前CG选择公式

\[
M_g=\begin{cases}
NoPred&S_g>0\;\land\;g>0\;\land\;H_{g-1}>0\\
Current&\text{其他情况}.
\end{cases}
\]

- TU开始S=0、H_prev=0，CG0仍Current。
- 进入CG时冻结M_g；不能用当前CG的d/G/H再选自己。
- CG结束更新S与H_prev；如果CG最终清零或没有有效位置，G=B=H=0，下一CG不得沿用更早的通过证据。
- 不跳过全零CG来寻找“最近一个有利CG”；最近就是扫描顺序中的g−1。
- 第一个已完成CG就可能提供足够的两个以上贡献，**不要求等到两个历史CG**。两CG TU的第二CG仍有使用机会。
- 单CG TU没有历史，NG/NGY完全使用Current。状态不跨TU、component、frame或被放弃的搜索分支传播。

源码中的C_log配合现有f还有一个简单性质：正d只能是2。
原因是NoPred有利只可能来自0<a<p时a映射为a+1，跨2的幂时C_log增加2，其余为0；
命中a=p时Current更便宜，贡献不为正。
因此G>0时B=2，新条件等价于上一CG的**净代理优势严格大于一次正贡献**，不是扫描QP后拟合的阈值。
实现仍建议显式累计B，便于核对规则；不将它称为“2 bit CABAC门槛”。

### 6.3 这组实际检验什么

旧R2-N只看S>0；NG要求最近CG也提供不由单个贡献支撑的正证据。
例如S=2且上一CG全零时，旧score仍可停留2；NG用H=0阻止继续选择NoPred，但不改score更新式。
这同时包含“近期证据”与“删一稳健性”，本组不能单独归因于其中一项。
若NG有效，后续才追加H替换为G>0的fresh-only消融，区分是否真的需要删一条件。

可能失败：增加保护错过有价值的单次证据；过去CG即使很有把握，也不代表当前CG；
稀疏历史可能让工具基本退回Current。以上均必须报告，不能因选得更少就宣称选得更准。

## 7. R3-2/4：分量作用范围消融

RGY/NGY仅在COMPONENT_Y执行对应新方法；Cb/Cr直接走Current预测路径，不更新替代predictor历史。
不是NoPred，也不是关闭色度TS。

这是有意较粗的消融：同时撤销U和V干预，可能损失原有V收益。采用它是为了少量、清晰地检验色度介入的净作用，
而不是断言U处处失败。不同时扫描Y-only、UV-only、U-off、V-off等组合。
如果结果支持保留色度，再研究具体分量机制；不事后将每条序列的最佳分量拼成新算法。

Y-only编码的U/V PSNR及总码率**可以改变**，因为划分、量化与参考重建存在反馈。
正确性要求是对应predictor调用返回Current，不是要求整条色度重建与anchor一致。

## 8. 首轮不做的方案

- 不再试CU-QP<=27/30/32/34的门槛搜索；原q32已说明名义QP曲线结构不能直接转换成CU规则。
- 不按“BQMall关闭、PartyScene开启”或class启用。
- 不增加虚拟CABAC先验/衰减系数/多候选数量的联合搜索。
- 不改跨TU历史，不改CG大小、scan、语法或Rice参数。
- 不因某个高QP结果等于anchor就放宽保护；先检查真实作用范围与是否确实没有可用证据。
- 不声称删一贡献是最优阈值或更准确分布估计；它是一项固定、可被否定的低复杂度假设。

## 9. 实现约束与宏接口

已在TypeDef.h增加 `JVET_BJUT_TS_R3_MODE`：0=不选R3，1=RG，2=RGY，3=NG，4=NGY。
沿用 `JVET_BJUT_TS_FIXED_PREDICTOR` 总开关；R3默认模式与旧fixed/revision1/R2及旧反事实分析宏互斥。
全模式0仍为Current。显式模式请求优先于编译默认；总开关OFF但请求实验必须报错。
不靠CMake设置实验选择，不修改旧实验编号或语义。

预期实现位置：

- `TsFixedPrediction.h`：局部winner保护、CG证据条件，复用既有f/C_TS/C_log。
- `ContextModelling.h/.cpp`：分量作用范围、CG开始冻结模式、最终CG统计G/B/H，私有预算精确回放。
- `QuantRDOQ.cpp`：复用最终CG清零决策后的历史更新钩子，确保搜索分支状态隔离。
- Writer/Reader：复用同一预测与状态代码，验证逆映射、扫描边界和状态同步。
- final Writer统计：单次计数，不把多遍调用或encoder临时搜索当成重复样本。

BDPCM/native bypass/cutoff=0保持identity；普通transform和TSRC关闭路径不改变。
不读取真正CABAC上下文来决定模式，不将任何诊断仿真写回真实状态。

## 10. 先验证作用机制，再正式编码

### Phase 0：实现正确性与短帧覆盖，不据结果调阈值

1. Current/master-OFF对保留anchor bit-exact；R2-R/N在新代码中对旧实现回归bit-exact。
2. 宏互斥/越界/总开关OFF误配置测试；Encoder/Decoder banner明确模式和R3版本。
3. 局部例子和CG贡献例子的独立单元测试；删除任意一项后固定候选cost优势仍正的断言。
4. 原生实际scan所有相关矩形/小尺寸；n不足、p=0/1、边界、Rice escape、bit depth、DQP、lossless、BDPCM。
5. 编码器/解码器逐CG模式、S、G、B、H、预算及q哈希一致；all-zero CG更新、TU回滚和未来q污染测试。
6. 明确产生“局部保护接受/拒绝”“NG在CG1就启用”“空CG后禁用”“Y-only色度走Current”的覆盖样例。
7. 统计开/关bit-exact；TS关闭与TSRC关闭时各实验与Current一致。
8. 代表性真实内容短帧检查BasketballDrill、BQMall、FourPeople、KristenAndSara，四QP均覆盖；
   短帧只用于活动度和机制诊断，不据它的RD优劣删除正式CE序列。

若保护几乎从不通过，应记录为覆盖不足，不能将“接近anchor”解释成成功，也不现场调整规则凑活动度。
正确性未通过不启动正式CTC；有活动不代表收益已证实。

### Phase 1：LB CE，四组全量半帧

- C4+E3全部保留，QP22/27/32/37。
- 采用既定LBeu半帧：BasketballDrill/PartyScene各250帧，RaceHorsesC150帧，其余四条各300帧。
- 每组28任务，四组共112任务。不得只跑QP22/27或只报有利区间。
- 不输出重建YUV；逐任务解码hash；复用现有共享任务池与每组完成即写表功能，不设置组间等待屏障。
- 复用现有Current Reference。保存远端源码/patch、宏、二进制SHA、命令、有效模式banner、输入/配置、帧数及状态。
- 新工具正式评估不补缺失点。缺点可展示暂估，但不能用于晋级、选winner或宣称达标。

复用R2结果的可比性按用户提供的外部配置记录确认；必要时小量哨兵校验需单独说明，不能悄悄启动完整anchor或对照重跑。
若无法确认，只能把跨轮增量结论标为条件性，不把旧表格当成新同版本随机对照。

### Phase 2：最多两组做RA CD，再按结果决定B

完成全部四组CE后，每个机制族最多保留一个候选，不逐序列选不同winner。
预先使用下列保守资源准入条件：

1. 完整CE平均<0，中位数<=0，去除PartyScene后均值<=0；
2. 对其既有R2父方法，低质量区间平均差值<0，且高质量区间仍有平均收益；
3. 不能仅靠所有作用消失回到Current；有可核对的接受/拒绝和remapping改变；
4. 纯保护组若相对父方法无增量，不因复杂度增加而优先；只有Y-only组通过时标记消融归因未完备。

这些是新R3预先声明的资源规则，不改写R2是否通过其旧协议的事实，也不是所有未来方案的理论必要条件。
同族均通过时按完整CE主指标选一组；精确平局优先全分量组，避免仅据相同RD增加分量例外。
没通过就停止扩大，不临时换成class均值、cubic或去除别的序列。

- 最多两组RA CD：每组32任务，共最多64。实际帧数必须与既有RA anchor匹配；RAeu默认短段不能冒充完整CTC。
- RA CD均值<=0才进入本轮计划的B测试；缺失/来源不明时暂不作通过判定。
- 最多两组LB B：每组20个半帧任务，共最多40。最终合并BCE十二序列评估−0.05/−0.08/−0.10%门槛。
- 首轮112，后续最多104，总计最多216个新正式编码任务，不含短帧验证及单独决定的消融补测。

B/RA此前已有其他算法结果，不能称为完全未见内容。若有候选达到目标，进一步用未参与本轮设计的内容/条件确认，
不能把在CE反复设计后的bootstrap区间当作完全独立验证。

## 11. 统计：同时报告收益、代价与作用范围

### 必须保留的RD分析

- Current主比较：逐序列、C/E/B、Y/U/V及6:1:1、完整四点PCHIP、cubic敏感性。
- 原四点曲线的高/中/低质量区间，不重新两点拟合，不把区间均值当作实际QP门控收益。
- 新方法/父方法的区间增量采用Current、父方法、新方法三方共有质量范围截取端点，
  内部分界仍为Current的QP32/27分量PSNR；避免两个区间端点不同造成假差异。
  单独对Current的主BD仍按原本双曲线公共区间计算，不改变正式口径。
- 中位数、标准差、分位数、逐一去序列影响度、按整序列配对bootstrap。
- 同QP同时展示码率与三个PSNR，不把单独码率下降当RD改善。
- 保护前后与分量消融：既报告各自对Current的主BD，也直接积分方法间RD曲线作辅助配对。
- 高质量收益保留率：父方法该区间均值b_parent<0时，rho_H=b_new/b_parent；
  它是效果保留诊断，不是Oracle capture ratio，可能为负或>1；接近零的父收益不作稳定解释。
- 低质量改善量：b_new−b_parent，负值表示改善。与rho_H共同判断是否只是全面缩回Current。

NG/NGY近似Current但无净收益只能评为保护过强/证据不足，不叫成功。
NoPred/directional旧结果仅辅助权衡复杂度；作用分量不相同的工具不能用一次比较证明历史选择本身更好。

### 默认在线聚合的机制数据

按sequence/config/nominal QP/component/W×H/CG数/CG位置/实际CU QP/intra-inter组织，保留真实尺寸，不写死8×8。
可采用多张边际分组表控制行数，不默认把全部维度连同每个POC做笛卡尔积。

- 最终coded TSRC TU、CG、nonzero、实际regular remapping位置、bypass数量；排除的CBF0/TSRC-off范围明确。
- 原方法提出alternative数量、新保护通过/拒绝数量、实际predictor变化与mapped变化，避免p=0/1等价造成虚假活动。
- RG：n、G、B、H的固定分桶，winner类别(Current/identity/other)，按分量与尺寸分层。
- NG：S符号、前CG G/H符号、NoPred CG数、被G−B条件挡住的CG数、无有效历史/全零历史数。
- 单CG、两CG、多CG TU分开；首CG和后续CG分开；不得只汇报可启用位置。
- 有效改动而非仅选择次数作为覆盖度；最近证据和净收益的关联作为诊断，不据同一数据反复拟合规则。

短帧可另开限量真实context克隆仿真，检查被保护挡住的局部候选在该固定q条件下是否常有代价。
它只能是条件性机制诊断，不替代闭环RD，不把最终TS样本当成全部搜索候选，不算跨闭环结果的CG级“真实因果收益”。
不同模式划分/q会变化，不能按同一坐标强行配对CG。

不默认输出逐系数文本。若每任务聚合1万行、每行约300字节，约3MB/任务，112任务约336MB；
这只是预算示例，实际行数需预跑测量。超过预算就减少默认联合作用维度，保留完整在线总和；
少量CG调试单独限额，不能为了日志上限偷偷丢统计样本。

## 12. 提前定义失败与后续动作

| 结果 | 解释与动作 |
|---|---|
| 全部接近Current且几乎无mapped改变 | 规则覆盖不足，不将“退化减小”当预测准确性提高；降低该规则优先级 |
| 低质量改善但高质量收益也基本消失 | 泛化成保守退回anchor，没有解决选择问题；不据均值近零宣称成功 |
| RG/RGY都不优于原R2-R | 否定本轮局部删一保护，不继续扫描删除项数 |
| NG/NGY都不优于原R2-N | 否定本轮近期稳健证据规则，不马上增加更复杂CABAC模型 |
| 只有Y-only有效 | 分量范围是线索；下一轮最多补一个对应未保护Y组，区分收益究竟来自哪里 |
| NG有效但归因不清 | 下一轮最多补fresh-only：S>0且G_prev>0，判断删一条件是否必要 |
| 只在PartyScene/Cactus等个别序列有效 | 不满足稳定性扩展；不为该序列加专用开关 |
| CE尚好但RA/B反转 | 降级为条件性效果，不泛化到正式通用工具 |
| 闭环有收益但分量/size机制无法解释 | 如实报告作用，不用固定q统计补造因果解释 |

后备消融不是现在就追加的实验清单，也不允许根据哪个结果更好事后挑选性披露。
若均失败，整体方向继续降级；若有稳定BCE收益达到用户目标、RA无系统退化且复杂度可接受，才评为Promising。
本设计不保证任何方法达到目标。

## 13. 实现交付状态（2026-09-19）

- 已定义四组公式、对照、因果/更新时机、scope、统计和晋级/证伪规则。
- 四模式已接入共同TS-RDOQ/Writer/Reader路径；TypeDef.h宏优先、运行时显式覆盖及新旧互斥已实现。
- 独立构建在 `build/ts-r3/`，宏OFF校验构建在 `build/ts-r3-off/`；交付源码master=1、模式全0，默认仍Current。
- 247项合成短帧、4项统计关闭、13项旧模式码流回归通过；原生scan及最终CG因果/预算/分量隔离测试通过。
- 原batch仅增加模式名，112项LB CE半帧dry-run通过；未启动这112项正式编码。
- final Writer在线聚合和基础RD分析已接入；具体字段及尚未单列的诊断分类见实现/验证文档。
- 结果入口：`experiments/ts_predictor_r3/<mode>/LB_CE/`；仅收件README，不生成空白结果表。
- 真实序列四QP短帧预检尚未完成（本地默认输入路径无对应文件），不得将合成覆盖当成CTC覆盖或收益证据。
- 正式结果齐全后再做跨轮共同质量区间消融；不根据本地smoke改冻结规则或提前选winner。
