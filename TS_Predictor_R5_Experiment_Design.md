# R5：在不改动 R3-1 的前提下检验候选排序与保守否决

版本：R5-20260921-v1。两个独立实验，**不组合**；Current 是唯一正式 anchor，R3-1 是增量对照。
实现与初步正确性验证见 `TS_Predictor_R5_Implementation.md`、`TS_Predictor_R5_Validation.md`。
本轮没有正式 CTC/BD-rate 结果，不把“实现完成”标为算法改进成功。

## 1. 数据依据与预检中的反证

依据 `TS_Predictor_R3B_R4_Results_20260921.md`：R3-1 BCE 暂估 −0.075273%，仍缺四个 B QP22；
R4-1/2 删除 identity/幅值分支均削弱 CE；R4-3 CE 完全同 R3-1；R4-5 改善部分序列，却损伤 PartyScene/BQMall 及低质量区间。
因此保留两类候选、YUV 范围、因果邻域和 Current 回退，仅分别改变排序或引入二选一否决。

**重要预检发现：**前一轮讲解提出“按 H 排序可能重排 R3 已接受的 winner”，只是待检验假设。
本轮 3,145,728 个小幅值模板/Rice 组合中，重排计数为 **0**，只出现 2520 个回退补查；
覆盖幅值至 32767 的人工网格也没有出现已接受 winner 重排；上述测试内与 R4-3 的预测差异也均为零。不能继续把 R5-1 宣传为已有有效覆盖的新机制。
这不是全动态范围等价性的数学证明，但足以降低长跑优先级。原先要求重排出现的覆盖检查失败，被保留为负面发现，未修改公式以强行通过。

R5-2 在 192,000 个独立网格目标中只接受 3 次否决；说明机制存在，但覆盖可能太少。
不得为了产生码流差异而降低 H>0、减少支持数或挑选有利视频后再称普适。

## 2. 共用不变量

- \(p_C=\max(L,U)\)；T+ 为 L/U/D/LL/UU 中非零幅值，重复值保留；不足三项时 Current。
- 候选为 0、Current 与邻居幅值；p=0/1 同属 identity remapping，NoPred 不是 anchor。
- \(f(0,p)=0\)，命中正幅值映射 1，0<a<p 映射 a+1，a>p 不变。
- 沿用现有 `syntaxCost`，其 Rice remainder 长度真实，但总分仍是整数语法代理，**不是 CABAC fractional bits**。
- Rice 与动态范围为 decoder-known 不可变 TU 参数；评分不依赖 Writer 多遍调用的临时 regular-bin 预算。
- 单个位置的预测不使用当前目标幅值、原始残差、失真、搜索信息、未来位置、QP门控或持久历史。
- 全部为逐系数局部因果方法，可以读取当前 CG 已知邻居，不冒充纯“前 CG 决定整 CG”工具。
- 不改 scan/CG 大小/语法/Rice/BDPCM/non-TS；只在原生 remapping 生效位置应用。

记对候选 p 的固定样本贡献：

\[
d_i(p)=C(f(a_i,p_C))-C(f(a_i,p)),\quad
G(p)=\sum_i d_i(p),\quad B(p)=\max(0,\max_i d_i(p)),\quad H(p)=G(p)-B(p).
\]

这只是固定候选的删一贡献敏感性，不是交叉验证或统计置信区间。

## 3. R5-1：margin-first

运行名 `r5_margin_first`；`R5_MODE=1`；目录 `r5_1_margin_first`。

原 R3：先按 G 选 winner，只有其 H>0 才接受；拒绝后不再搜索。
本组：对全部原候选计算 G/H，在 H>0 集合内按 **H 最大、G 最大、identity 优先、较小幅值优先**选择。
没有 H>0 候选则 Current。Current 不需要作为非零增益候选参与循环；其 G/H 均为零且零优势必须回退。

规则上不提前保留 R3 已接受选择，这是与 R4-3 的定义差异；**实际可用输入是否出现这种差异必须实测，不从代码分支推断**。
在固定输入上若 R3 已接受，则它属于新规则的可行候选，R5-1 选中 H 不会更小；这不保证 CABAC 或闭环 RD 更好。
H 和 G 的候选扫描仍有选优偏差，不能把代理下界当实际率收益下界。

独立输出：accepted_parent_reordered、fallback_rescued、p_vs_r3、remap_vs_r3。
若只观察到继承 R3 的活动，或行为只复现旧 R4-3，则停止本版正式扩展；目前证据正指向这一风险。

## 4. R5-2：Current-only causal veto

运行名 `r5_current_veto`；`R5_MODE=2`；目录 `r5_2_current_veto`。

先计算原 R3-1 输出 \(p_R(i)\)。若与 Current 等价则直接保留，不做无效历史回看。
否则对 T+ 中每个历史目标 j：

1. 用 **j 自己更早的因果邻居**计算原 R3 预测 \(p_R(j)\) 和 Current \(p_C(j)\)；不把 j 本身纳入它的输入。
2. 用已知 \(a_j\) 评价二者：

\[
d_j=C(f(a_j,p_R(j)))-C(f(a_j,p_C(j))),\quad
H_V=\sum_jd_j-\max(0,\max_jd_j).
\]

3. 若 \(H_V>0\)，当前位置用 \(p_C(i)\)；否则保留 \(p_R(i)\)。

历史 primitive 固定为原 R3，**不递归调用 R5**；历史评分统一用当前 TU 的固定代理参数，不回放过去目标真实 CABAC 状态/预算。
过去目标自身模板不足时，原 R3 自动为 Current，该样本贡献零，仍计入当前 T+。

输出仅可能为原 R3 或 Current；不切换到独立 NoPred/directional 专家，原 R3 内部 identity 候选仍保留。
这是 R4-5 模型集合的一个独立缩减实验，检验“否决”是否比“主动换其他专家”更可靠，不能预设后者就是 R4-5 的退步原因。
可能失败：过去 R3 已很保守，Current 几乎不胜出；或回退损失原本有利区域。两种都应报告，不调阈值追求覆盖。

## 5. 闭环、因果性与统计边界

QuantRDOQ / Writer / Reader 复用同一公共预测入口。量化系数和最终 TS 决策允许改变，是闭环实验，不是固定 anchor 最终 TS 集合的反事实主结论。
不持久保存邻居 q 或评分，RDOQ 临时 CG/整组清零/被放弃分支不会污染以后历史；写码流与解码时从实际系数重新计算。
新方法只读幅值，不需要 R4 signed-plane 的 sign 视图。

最终 Writer 默认输出聚合 `TS_R5_STATS`，不逐系数打印；`TS_R5_STATS=0` 关闭观察。
按分量/W/H/CU-QP/intra/BDPCM/CG数/CG位置聚合，序列及名义QP由外部任务元数据关联。
只统计进入 TSRC 的最终块；CBF0/TSRC-off不属于此入口。内部活动仍是条件样本，不当作无偏 Oracle、η 或实际 CABAC 率。

增量活动必须看 **remap_vs_r3**，不能拿 remap_different（对 Current）代替。
模式1 accepted 表示选中非 Current 的 H>0 候选；模式2 accepted 表示通过 Current 否决，两个分母/含义不同。
模式1最大候选未通过时记录 G/H=0，不能把其零桶解释成“所有候选都零分”；模式2保留实际否决评分正/零/负桶。
记录相对 Current/R3 的目标事后代理损益，仅作机制诊断，不回流选择。

## 6. 冻结的验证与后续编码门槛

先完成公式/因果/native CABAC往返、Current/OFF bit-exact、旧模式回归和stats-off一致性。
局部人工例能触发不等于视频中有有效覆盖：必须同时看候选提议、接受、p改变、实际remapping改变及码流变化。

1. 若代表性真实短测没有相对 R3 的有效映射改变，**先停止扩大编码规模**；不因正确性通过就启动完整 CE。
2. 若活动有证据，再分别做 LB CE 半帧 QP22/27/32/37，各28点；不重跑完整 Current anchor，不输出重建。
3. 完整 CE 必须按各分量 PCHIP 后 6:1:1、七序列等权；R3为增量重新积分对照，Current仍唯一正式基线。
4. 扩展要求：对 Current CE均值<0、中位数<=0、去Party<=0；直接对 R3-1 均值<0、中位数<=0；三曲线共同低质量区间不劣、高质量仍有Current收益；活动/远端身份/解码hash明确、复杂度可接受。
5. 本轮最多一个候选扩展，按直接对R3均值排序，平局考虑Decoder开销；CE是重复开发集，不算独立泛化证据。
6. R3剩余B点补齐后再定推广；BCE按十二序列等权，−0.05/−0.08/−0.10%工程门槛不变。

不组合两组、不调样本数/H阈值、不按QP/class/序列开关、不根据单个好例改公式。
若两组均弱或无活动，应明确否定这轮细化方向，下一轮另立评分/统计假设，而不是把同一组悄悄改成更激进算法。
