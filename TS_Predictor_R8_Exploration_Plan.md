# TS predictor R8：机制驱动的多组探索实验

**状态：算法与实验设计，尚未修改或运行编码器。** R8、A01–C04 是本设计的新编号，不是现有源码已支持的运行参数。数学参考测试已经运行，但不能替代编解码正确性验证。默认全 YUV、全部原生支持的 TS 尺寸，保持原有 BDPCM 排除方式，不改变量化参数、lambda、测试配置或标准锚点。

## 1. 修正研究出发点

R3-1 是当前样本上的强对照，不是已经证实的总体最优算法，也不是所有后续方法必须保留其已接受分支的约束。旧规则在旧评分、旧候选集和原量化搜索下表现不佳，不等于其在新的组合下无效。R7-1 对 R2-2 改善而 R7-2 对 R7-1 退化，已经提示评分与接受规则存在交互；它不证明哪一项在所有条件下正确。

固定输入、二进制和确定性运行环境下，重跑同一任务不产生独立的算法效果样本。这里要研究的是有限内容集和初始化条件下，细小决策变化经过闭环产生的路径敏感性；真实的持续参考改善同样会经闭环发生，不能把所有传播一律扣成噪声。当前数据不能分解出 R3-1 的收益有多少来自算法、有多少来自偶然有利的路径。

Y/U/V BD-rate 使用相同的整码流码率轴。即使 UV 重建相同，Y 比特变化也可改变 UV BD-rate；此外 Y 的模式与重建变化可能改变后续共享决策。因此不从 UV 的指标退化直接推出 UV predictor 有害，也不拼接不同运行的分量结果。

## 2. 共同符号及代码对应

当前系数的五个因果位置是 L、U、D、LL、UU。它们的非零量化幅值组成 a_1,...,a_n，0<=n<=5；不同位置的重复幅值保留为多份样本。p_C=max(|L|,|U|) 是 Current 的预测幅值。

映射：

$$M(a,p)=\begin{cases}0&a=0\\1&a=p>0\\a+1&0<a<p\\a&a>p.\end{cases}$$

p=0 与 p=1 的映射等价，统一为 identity。预测幅值必须处于合法动态范围；边界点、矩形块和跨 CG 可得性不变。

C_I 是 TsFixedPrediction.h 的 syntaxCost；C_F 是 TsRateCost.h 的 CG 入口冻结 fractional cost，第一批保持 cutoff=10。参与混合时将 C_I 左移 SCALE_BITS，与 C_F 使用相同单位。除 C03/C04 指定的量化搜索外，本轮先只修改公共预测器，不偷偷更换 RDOQ 的其它估计器。

设 l_i(p)=C(M(a_i,p))，S(p)=sum_i l_i(p)。原候选集 P0={0,a_1,...,a_n}，包含 p_C；去重时保持 Current 优先、然后 identity、然后较小 p 的平局规则。

原 guard：先取 p*=argmin S(p)，再计算

$$G=\sum_i[l_i(p_C)-l_i(p^*)],\qquad H=G-\max(0,\max_i[l_i(p_C)-l_i(p^*)]).$$

H>0 才接受不同于 Current 的候选。这是删除一个最大有利贡献的敏感性规则，不是置信概率或独立交叉验证。

### 2.1 理论目标与现有近似

对固定的因果状态 h、冻结概率状态 s 和固定语法路径，局部目标是

$$p^*=\arg\min_p\sum_a P(A=a\mid h)C(M(a,p);s).$$

当前用五邻居的经验频率代替目标分布 P。更准确的 CABAC 概率主要改善 C 的估计，不能自动修复小样本分布误差、候选覆盖不足、实际语法路径变化或量化搜索耦合。上述目标也不等于完整 TU RD，更不等于整序列闭环目标。

## 3. A 组：精确评分与 R6 的直接交叉组合（12 组）

### 3.1 四种稀疏支路

只在 n<3 时使用：

- S0：保持 Current。
- Smax：只有 n=2 且 L、U 都非零时取 max(|L|,|U|)，其余取 0。
- Smean：上述双直接邻居条件下取 floor((|L|+|U|+1)/2)，其余取 0。
- Smin：上述条件下取 min(|L|,|U|)，其余取 0。

这与原 R6-5/6/7 的稀疏规则一致，但 dense 部分由各实验重新指定，而不是固定保留旧 R3。

### 3.2 六种 dense 支路

全部使用同一 C_F 及 P0。

Raw：直接选择最小 S，无额外 guard。

Guard：使用上述 G/H。

All-unaccepted-to-0：n>=3 时，只有 Guard 接受了一个非 Current 候选才保留它；其余全部选 0。对应 fractional 版 R6-1。

Rejected-to-0：原始赢家等于 Current 时仍用 Current；非 Current 赢家 H>0 时接受，H<=0 时选 0。对应 fractional 版 R6-2。

Trim-cost：直接最小化

$$T_3(p)=\sum_i l_i(p)-\min_i l_i(p).$$

对应 fractional 版 R6-3，不再叠加原 guard。

Trim-saving：直接最小化

$$T_4(p)=\sum_i l_i(p)+\max(0,\max_i[C_F(a_i)-l_i(p)]).$$

对应 fractional 版 R6-4，不再叠加原 guard。该式等价于以 identity 为参考删除一个最大的正节省贡献。

| 编号 | dense 决策 | sparse 决策 | 主要检验 |
|---|---|---|---|
| A01 | Raw | Smax | R7-1 与 R6-5 融合 |
| A02 | Raw | Smean | R7-1 与 R6-6 融合 |
| A03 | Raw | Smin | R7-1 与 R6-7 融合 |
| A04 | Guard | Smax | R7-2 与 R6-5 融合 |
| A05 | Guard | Smean | R7-2 与 R6-6 融合 |
| A06 | Guard | Smin | R7-2 与 R6-7 融合 |
| A07 | All-unaccepted-to-0 | S0 | 精确评分是否改变 R6-1 判断 |
| A08 | Rejected-to-0 | S0 | 精确评分是否改变 R6-2 判断 |
| A09 | Trim-cost | S0 | 精确评分是否使 R6-3 有效 |
| A10 | Trim-saving | S0 | 精确评分是否使 R6-4 有效 |
| A11 | Rejected-to-0 | Smax | 同时调整 dense 拒绝与 sparse 支路 |
| A12 | Trim-saving | Smax | 对称 robust 思路与 sparse 规则组合 |

R7-1=Raw+S0、R7-2=Guard+S0，是现有两个对照。A01–A06 与它们形成 2×4 的接受方式/稀疏规则矩阵，而不是仅测试已经在旧评分下表现最好的 max。

旧 R6 对照可用于观察“同一规则换评分”的结果，但必须核实源码、输入、配置、帧范围身份匹配。禁止把多个 BD-rate 差值简单相加，预测组合收益。

## 4. B 组：重新设计评分及候选机制（8 组）

### 4.1 B01/B02：整数—fractional 评分收缩

$$C_{mix}(a)=\tfrac12 C_I(a)+\tfrac12 C_F(a).$$

实现时按共同定点单位相加即可，无须真的除以 2。它不是“更精确的物理码率”，而是一个明确的正则化假设：概率自适应评分与小样本分布误差共同存在时，纯 fractional 未必具有最好的决策泛化。固定 1:1 是本轮预注册试点，不按序列或 QP 调参。

B01：混合 cost、Raw、P0、Smax。
B02：混合 cost、Guard、P0、Smax；G/H 同样使用混合 cost，不混用不同单位或不同成本来源。

### 4.2 B03/B04：补齐所有不同的样本映射状态

fractional cost 包含奇偶概率和不同 greater-than bin 的概率，不保证随幅值单调。原候选集只有已观测幅值和 identity，可能漏掉局部评分真正的最优解。

将候选扩为

$$\boxed{\mathcal P_1=\{0\}\cup\{a_i,a_i+1\}_{i=1}^n}.$$

按合法最大预测幅值裁剪、去重，并将 0/1 规范化。最多 11 个候选。

**覆盖证明。** 设不同正幅值为 v_1<...<v_m。当 p=v_j 时出现该幅值的命中映射；当 v_j<p<v_{j+1} 时，每个样本均不命中，且小于 p 的样本集合不变，因此整个映射向量 (M(a_1,p),...,M(a_n,p)) 不变。每个整数非空间隔可由 v_j+1 表示；最大幅值以上的区间同样由 v_m+1 表示。最小幅值以下区间由 identity 表示。因此 P1 覆盖合法整数 p 能产生的全部样本映射向量。固定 cost 下，任何只依赖该映射向量的评分，无须搜索更大的 p 集合。

这是**固定样本、固定 cost 下的覆盖结论**，不是完整 CABAC 路径、下一系数或闭环 RD 的最优性保证。

**可复核的构造示例，不是实验测得的 CABAC 状态：** 取三个样本 [3,3,3]；令 gt1 的 bin=1 概率为 0.8，parity 的 bin=1 概率为 0.1，第一个 greater-than 的 bin=1 概率为 0.8，第二个为 0.1。依照 RateTable 的 bin 顺序可得 C_F(1)=2.321928、C_F(3)=5.965784、C_F(4)=0.947862 bit。原候选最优 p=3 的总 cost 为 6.965784；新增 p=4 的总 cost 为 2.843587。因此“不命中邻居幅值”的候选也可能更便宜。合法概率的构造只证明不能在理论上排除这一情况，不证明它在当前码流中高频发生。

B03：C_F、Raw、P1、S0。
B04：C_F、Raw、P1、Smax。

必须记录新增候选选择次数、真实 remapping 改变次数、真实量化系数变化次数及最终提交次数，不能只记录候选被遍历。

### 4.3 B05/B06：对所有候选对称的删一样本最坏后悔值

原 guard 先固定一个赢家，再与 Current 比较。这里不固定 Current 为唯一受保护的解。

对完整样本及每一种删除一个位置的情景 j=0,...,n，计算

$$S_p^{(0)}=\sum_i l_i(p),\qquad S_p^{(j)}=\sum_{i\ne j}l_i(p),$$

$$r(p)=\max_{j=0,...,n}\left[S_p^{(j)}-\min_{q\in\mathcal P}S_q^{(j)}\right].$$

首先最小化 r(p)，再最小化完整样本 S(p)，最后按统一平局规则决策。不要额外叠加原 H>0 guard。

该方法问的是：“任意一个邻域观测不可靠时，这个预测器距当时最好的候选最多损失多少？”它不要求每个情景都获胜，也不会把 Current 自动视为正确。它是有限情景 robust 决策，不是留一交叉验证或统计显著性检验。

复杂度：预先生成 l_i(p) 表，每个情景的最佳值与每个候选的最大 regret 均可在 O(n|P|) 内得到，不需要候选两两比较。

B05：C_F、minimax regret、P0、Smax。
B06：C_F、minimax regret、P1、Smax。

### 4.4 B07/B08：邻域幅值分布的三点平滑

现有经验分布将所有概率压在已出现的幅值上。在很小的邻域中，这可能使精确的命中位成本被过度利用。试验一个明示的局部分布假设：与已观测幅值相近、相差一级的目标系数也有概率。

$$S_{smooth}(p)=\sum_i\left[C_F(M([a_i-1],p))+2C_F(M(a_i,p))+C_F(M([a_i+1],p))\right],$$

方括号表示裁剪到合法非负幅值范围，权重为 1:2:1。边界裁剪后合并相同幅值的权重。整体除以 4 不影响排序。零幅值的公共 significance 等项与 p 无关，本局部 remapping 排序可忽略，不能因此将零事件当成真实编码零成本。

令平滑后的正支持集合为 V，再使用 {0}∪{v,v+1:v∈V} 作为候选集；等价地最多包含原样本附近 a_i-1,a_i,a_i+1,a_i+2 与 identity，共不超过 21 个候选。此时不能只扩大评分分布却仍使用旧 P0，否则人为限制了新模型。

B07：n>=3 使用平滑 Raw，n<3 使用 Smax。
B08：n>=1 全部使用平滑 Raw，n=0 才用 identity；直接尝试替代硬 support>=3 门槛。

±1 和 1:2:1 是一个待证伪的量化幅值平滑假设，不是已从数据证明的真实条件分布。不在首轮扩展成多带宽、多 QP 阈值扫描。

### 4.5 B 组汇总

| 编号 | cost/分布 | 决策/候选 | sparse |
|---|---|---|---|
| B01 | 半整数＋半 fractional | Raw/P0 | Smax |
| B02 | 半整数＋半 fractional | 同 cost Guard/P0 | Smax |
| B03 | fractional | Raw/P1 | S0 |
| B04 | fractional | Raw/P1 | Smax |
| B05 | fractional | 对称 minimax regret/P0 | Smax |
| B06 | fractional | 对称 minimax regret/P1 | Smax |
| B07 | 三点平滑 fractional | Raw/平滑完整候选 | Smax |
| B08 | 三点平滑 fractional | Raw/平滑完整候选 | n>=1 同一规则 |

## 5. C 组：语法路径与量化搜索协同（4 组）

### 5.1 C01：双 regular 路径 cost

现有 RateTable 支持 cutoff=10 和 2，但 ratePredictionTS 的公共评分默认只用 10。先试

$$C_{path}(a)=\tfrac12 C_{10}(a)+\tfrac12 C_2(a).$$

其余固定为 Raw、P1、Smax，与 B04 形成单因素比较。固定一半一半是路径敏感性试验，不宣称为校准后的实际路径概率。

纯 bypass 不进行当前幅值 remapping，因此其候选无关部分应使用未映射的 a，不能写成 C_0(M(a,p))，也不必计入候选排序。候选本身还会影响实际 regular-bin 消耗，所以混合 cost 仍是近似模型。

### 5.2 C02：仅用已完成 CG 更新路径权重

只改变 C01 的混合权重。每个 TU 初始化两个计数 w10=w2=1；在当前 CG 编码/解码开始前冻结它们。用

$$C_{path}(a)=w_{10}C_{10}(a)+w_2C_2(a)$$

排序，共同分母可省略。CG 完成后，根据最终非零系数实际经历的 cutoff=10/2 个数 N10、N2 更新

$$w_k\leftarrow\max(1,\lfloor w_k/2\rfloor+N_k),\quad k\in\{10,2\}.$$

完全没有 regular 非零样本的 CG 不更新计数。纯 bypass 不被错误计为预测器的收益样本。只允许先前已经完成的 CG 的路径进入下一 CG 的权重，禁止读取当前目标系数或当前 CG 最终 cutoff 来决定当前 predictor。

这是用于 cost 校准的两个计数，不是直接沿用旧版“上一 CG 谁赢就选谁”的专家切换。仍需检验跨 CG 分布漂移；若历史不稳定，C01 可能优于 C02。

Encoder 的每个 RDO 分支使用私有副本，最终 CG 确认后才提交更新；CG 清零、TU 放弃时回滚。Decoder 在 CG 最终还原后更新一次；Writer 的最终路径与 Reader 必须完全一致。

### 5.3 C03/C04：让量化候选适配新 predictor

上传 QuantRDOQ.cpp 中，额外 upAbsLevel 只有在不与原候选重复并且 remapping 后为 1 时才加入。由此 predictor 不只改变系数的 cost，也改变哪些量化值有机会被搜索。

第一版不引入 beam search，不改变零分支和 lambda。针对原 roundAbsLevel>0 的位置，将候选集扩为原集合∪{upAbsLevel}，无论该候选 remap 是否等于 1；去重、裁剪。对 roundAbsLevel=0 的原生提前返回先保持不变，以避免把零/非零搜索重写混入同一实验。

为了避免仅改善原来的不精确逐点目标，在同一 TU 入口状态分别生成原搜索 q0 与扩展搜索 q1；两者均使用该实验固定的公共 predictor，再完整重建并回放相关 TU 语法，比较

$$J(q)=D_{recon}(q)+\lambda R_{TU,frac}(q).$$

两条路径都必须重新生成其后继 predictor、CG 上下文、regular-bin 预算及 cutoff，不能把 q0 的后续状态套给 q1。使用现有重建失真/clip/分量权重口径和原 lambda，不另造一个与编码器不同的目标。

C03：固定 R3-1 predictor，比较原/扩展量化搜索。
C04：固定 A01 predictor，做相同量化搜索比较。

四个角 R3-1、C03、A01、C04 可检验 predictor 与量化搜索的交互。这不是在码流里无信令切换 R3/A01；每个实验只有一个固定公共预测策略，因此 Decoder 无须知道编码端尝试过哪些 q。

保留 q0 只能保证所定义的局部最终 fractional-RD 目标不升，不能保证实际字节数、其它工具竞争或整序列 BD-rate 不升。额外 TS RDOQ 路径的成本要实测；本组实现与编码复杂度高于 A/B。Decoder 不增加量化搜索。

## 6. 统一工程实现，避免产生伪实验

建议新增独立配置结构，而不是在 r6/r7 旧分支反复嵌套宏。概念字段包括：cost model、sample model、candidate generator、decision rule、sparse rule、component scope、quantization search。旧组的名称、默认值和行为保持不变。

重点位置：

- TsRateCost.h：抽象同单位 cost 接口，复用冻结表，增加候选与决策函数。
- TsR6Prediction.h：将仅整数版 r6Score 参数化为 cost callback；保留不同位置的重复样本。
- ContextModelling.h：统一分派、因果模板、范围和 snapshot 依赖。
- TsFixedPrediction.h：新增版本标识与配置，不让环境变量无声覆盖实验设计。
- CABAC Writer/Reader 与 QuantRDOQ.cpp：所有依赖 fractional 的新模式都启用所需 freeze/replay；不能只把 mode 32/33 视为需要概率状态的模式。
- C03/C04：数组容量、去重、error 下标和 testedLevels 同步更新；不是只向 coeffLevels[3] 写第四个值。

应有一个统一 needsTsRateContext(mode) 判定，以免 A/B 新模式调用未初始化的 RateTable。RDOQ 试算概率状态不必与最终 Writer 的状态被假定为相同；它可以是估计状态，但最终 Writer/Reader 的决策及重建必须一致。

### 6.1 复杂度控制

每次预测决策缓存 C(v)、C(v+1)；平滑组增加所需邻近幅值项。CG 概率冻结不变时可复用相关表，但当前直接邻居非零数会改变 context 分类，不能跨类别误用缓存。Raw/trim/minimax 共用同一 loss 矩阵。64 位整数累加，不在逐系数热路径调用 log、pow 或动态内存分配。

仅定点求和的缩放不影响 Raw、原 guard、trim 和 minimax 的结果；不要因为除法舍入改变平局。代数等价优化单独做 bit-exact 测试，不把它算作新增 BD-rate 算法组。

### 6.2 正确性必须检查

边界/矩形/不同 TS 尺寸；n=0...5；重复幅值；0/1 等价；最大动态范围；regular cutoff=10/2 与 bypass；CG 全零与重置；BDPCM；不同 compID；最后有效系数与 sign 处理；线程隔离；Decoder 与 Writer trace/hash；整个 TU 被其它模式淘汰后的状态回滚。

数学测试 verify_candidate_coverage.py 已检查 1287 个原样本多重集合、1287 个平滑支持集合的候选映射覆盖，并生成合法 Bernoulli cost 的反例。**它没有验证 C++ 编解码、运行时可达 CABAC 状态分布或任何 BD-rate 收益。**

## 7. 实验执行及因果诊断

### 7.1 基础对照

保留 Current、R3-1、R7-1、R7-2 四个对照；按实验比较对应旧 R6、B04、A01 等直接父对照。现有合法完整对照可复用；同名不代表相同输入、源码、二进制、宏、环境变量和配置。Reference 中此前发现的 RA BasketballDrive Q37 md5 mismatch 不能纳入正式四点结果。

所有主实验先使用全 YUV 和所有原生 TS 尺寸，不先加 Class、分辨率、QP、序列名称的特判。本设计不根据现有 U/V 指标强行设定 Y-only。

### 7.2 两层筛选，不以“首轮没赢 R3”一票否决

第一层是有效性：编解码正确、模式确实加载、算法确实有机会改变映射/量化/最终提交，状态和实际码率路径有效。完全无活动的组先修正机制或说明数学等价，不继续扩大无意义的远端测试。

第二层是研究价值：闭环结果、同状态局部目标、新增候选的真实作用和跨内容一致性共同看待。平均值暂时低于 R3，但具有不同错误分布或与其它因子互补的组，可以继续配对验证。不能把所有退化都归咎于随机性；若多种初始化/新内容上都稳定退化，且局部目标也不改善，再降低优先级。

短帧只做工程验证和机制定位，不作为最终排序。冻结旧 q 的 shadow 不用于排除可能通过新 RDOQ 路径获益的算法。

### 7.3 推荐批次

全部 24 组定义在 experiment_manifest.json。首批可先实现/运行 A01、A04、A08、A09、A10、A11、B01、B03、B05、B07，共 10 组，覆盖主要结构。其余组按共享实现展开，而不是要求“首批必须胜 R3 才允许验证其它方向”。A02/A03/A05/A06 对 mean/min 的复验是正式计划的一部分。

完整 CE 为 7×4=28 个点；24 组全部完成是 672 个测试编码点，不含可复用对照、正确性短测及后续验证。该数字只是任务量，不是运行时长估计。四个 QP 使用既有 22/27/32/37，从原有效配置读取实际帧数和 GOP，不推测未上传的配置细节。

B 集与新内容可用于下一阶段验证，但已有被反复查看的数据不称为严格盲测。应额外预先选定尚未用于规则设计的片段/内容，锁定算法后只做确认，不随结果继续调参。

### 7.4 应记录的指标

每条完整编码保存总码率、Y/U/V PSNR、分量 SSE、编解码时间和身份/hash。分量位成本优先记录逐语法 bin 的 fractional attribution，不把跨分量共享算术编码字节强行分解为精确独立码流。

算法诊断以 compID、n、直接非零个数、尺寸、QP、CG位置、实际 cutoff 分桶，计数以下链条：访问位置→候选集合新增→预测值不同→实际 remapping 不同→量化 q 不同→TU最终提交。另记录 guard 接受/拒绝迁移、各候选 score 与 winner margin、完整 TU 的 ΔRfrac/ΔD/ΔJ。只记录计数/直方图及少量限额样本，避免整帧逐系数巨大日志。

至少区分“给定同一 q 的熵代价变化”“给定同一 TU 输入的量化/RD变化”“整个编码闭环结果”。前两层用于解释，第三层用于最终性能，不能将三层数值混在一起。

### 7.5 评分—规则交互

对四个组合 (旧评分,旧规则)、(新评分,旧规则)、(旧评分,新规则)、(新评分,新规则)，首先分别重算直接比较。需要一个明确的交互数时，对同一序列、每分量在四条曲线共同 PSNR 区间 I 上定义

$$L_{ab,c}=|I_c|^{-1}\int_{I_c}\log R_{ab,c}(z)dz,$$

$$I_c=(L_{11,c}-L_{10,c})-(L_{01,c}-L_{00,c}).$$

I_c<0 表示按该统一口径存在更有利的联合变化；再按 6:1:1 和序列等权汇总。不能将使用不同公共积分区间的 BD-rate 百分比直接相减当成精确的交互。

### 7.6 Y/UV 交互，不作指标归因捷径

为两个机制不同的候选各做四角实验：

| 组合 | Y | UV |
|---|---|---|
| 00 | R3 | R3 |
| 10 | 新算法 | R3 |
| 01 | R3 | 新算法 |
| 11 | 新算法 | 新算法 |

00 和 11 已在主实验中，每个候选只需新增 10/01。四组全部重新闭环，不能拼接结果。先看总码率与原始 ΔSSE_Y/ΔSSE_U/ΔSSE_V，再解释分量 BD-rate。若 10 的 UV predictor 未变却 UV 指标变化，这是跨分量或共同码率作用的证据，但不自动说明具体由哪条链路造成。01 也可能反过来影响 Y 的共享决策。

需要进一步定位时，固定同一 TU 输入、预测、划分和其它决策，仅替换某分量的 remapping/量化做局部对照；该受控实验只解释直接作用，不等同于真实闭环性能。

### 7.7 闭环敏感性

先检查同任务重跑的 bitstream/reconstruction hash；若不一致，排查运行设置、并发和未定义行为，不把实现不稳定视为算法随机性。

随后对算法、Current 和 R3 使用同一套预先选定的合法起始位置/固定长度窗口，覆盖不同初始 I 帧与内容位置。窗口必须由实际配置和可用帧数确定，保持所有算法相同，不根据结果挑有利起点。同一原序列的窗口是相关样本，不能当成多条独立序列增加显著性。此测试是非标准初始化敏感性验证，正式 CTC 结果仍单列。

如需微扰实验，只作为独立诊断：在预定源位置做对称、小幅、有限的编码端干预，对算法及锚点采用相同定义，不改变解码规则；不得反复试直到选到正收益分支，也不得把最好 seed 当正式结果。本轮优先使用窗口/新增内容验证，避免引入 lambda 干预的额外混杂。

报告标准窗口结果、配对窗口结果、序列级/序列簇级分布、正负比例和区间，而不是只给一次均值。不要规定“所有序列都必须改善”或“拿掉任一序列都必须改善”才有研究价值。多重试验开发阶段的区间仅为描述性，不当作独立验证的显著性证据。

BD 插值与闭环敏感性分开检查：保留四 QP 主口径，绘制/查看相对 RD 曲线，必要时为最终相近候选增加中间 QP 支持点验证积分。不能套用一个通用百分比阈值断言小幅收益无效。

## 8. 本轮明确不做的事

不根据过去均值预先删除 R6-1/3/4 或 mean/min；不把 R3 当作必须不被覆盖的正确决策；不按序列名/类/旧数据赢家硬编码开关；不把 UV 指标变化直接归因于 UV 预测；不让公共无信令 predictor 读取当前未解码系数、未来 MV/CU 或当前 CG 最终路径；不把日志模板当真实运行记录；不使用失败点；不把上述新设计写成已经得到收益。

## 9. 来源与交付

源码依据：本次 source(20260925-022556).zip 内 TsFixedPrediction.h、TsR6Prediction.h、TsRateCost.h、TsRateReplay.h、TsR4Prediction.h、ContextModelling.h 和 QuantRDOQ.cpp。现有比较数据依据：TS_Predictor_Current_Analysis.md 与对应复算 CSV；本设计不重新宣称完成了一次原始全量编码。

BD 方法外部参考：Christian Herglotz 等，The Bjøntegaard Bible — Why your Way of Comparing Video Codecs May Be Wrong，arXiv:2304.12852，2023。这里只引用其对插值、支持点和相对曲线检查的讨论，不从文献的不同数据集误差范围推导本项目的统一收益门槛。

附带 experiment_manifest.json、verify_candidate_coverage.py、candidate_coverage_result.json。manifest 是开发规格，尚不能直接作为当前编码器配置执行。最值得同时推进的是：精确评分×R6交互、非单调 cost 下完整候选覆盖、对称稳健/平滑目标分布，以及 predictor×量化搜索协同；它们分别针对不同机制，不能在试验前由一次均值排定永久优劣。
