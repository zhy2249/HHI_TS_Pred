# R4：分解R3局部预测收益，并补全失败候选后的稳健选择

实施更新（2026-09-20）：六组现已接入codec/宏，见 [实现与使用](TS_Predictor_R4_Implementation.md)。
当前验证状态以 `TS_Predictor_R4_Validation.md` 为准；公式不变，文末单列设计阶段历史。

核心三组版本：R4-20260920-v1，公式冻结不变。当前总计划已扩展为v2：
[非消融扩展](TS_Predictor_R4_NonAblation_Extension.md)新增R4-4/5/6，不改变本文R4-1/2/3。
状态：**六组已接入codec/宏/batch，完成本地合成验证，尚未启动正式CTC。**
本文模式名已经实现，可作为batch参数；算法宏使用见实现文档。
R3继续按原版本测试；本轮不修改R3源码逻辑、二进制、门槛或结果。
依据：`TS_Predictor_R3_LB_CE_Results_20260920.md`。

## 1. 结论与实验矩阵

原核心设计包含三组；用户随后要求增加的三套完整新方法单列于扩展文档，不混入以下消融解释：

| 编号 | 拟议模式 | 作用 | 主要问题 |
|---|---|---|---|
| R4-1 / I | r4_identity_only | 只保留R3-1已接受的NoPred分支 | R3收益是否主要来自局部停用预测？ |
| R4-2 / M | r4_magnitude_only | 只保留R3-1已接受的非identity幅值分支 | 真正的幅值重预测是否有独立价值？ |
| R4-3 / C | r4_guard_rescue | R3接受则保持；R3拒绝后补查其他通过相同保护的候选 | 单一winner被否决后直接回退，是否漏掉了稳健替代？ |

三组均作用于Y/U/V，不另复制Y-only组。正式anchor仍是Current，R3-1是增量对照而非替代anchor。
固定NoPred只作既有辅助对照，不新跑完整NoPred/Current。

首轮三组LB CE半帧、四QP，共 **3×7×4=84个新正式任务**（未来由用户决定是否启动）。
前两组是分支消融，第三组才是主要的新选择方法；不能把三组都称为已知更优的预测器。

## 2. 从现有数据到可检验假设

| 现有证据 | 推出的问题 | 不能直接推出的结论 |
|---|---|---|
| R3-1 CE−0.09329%，去Party仍−0.03067%，优于R2-R | 保留已有效的局部保护，在它的结构内改进 | 更严格或更宽松一定更好 |
| 固定NoPred原CE−0.05672%，R3局部候选也含NoPred | R3的增量来自选择NoPred，还是选择新的正幅值？ | 不经消融就认定复杂幅值预测必要 |
| R3-1有五条相对R2-R改善，却丢失部分FourPeople/Kristen收益 | 保护后的候选选择仍可能不完整 | 已能锁定具体序列的失败根因 |
| R3-1/2的E十二点相同 | Y-only尚未解决E退化，不优先新增分量组合 | 色度工具永远没有价值或远端模式已独立认证 |
| R3-3/4丢失约一半以上高质量收益，扩展门槛未通过 | 暂停加码纯历史CG规则 | 已否定所有跨CG可预测性 |

现有表格没有winner分类/CG活动日志。I/M正是为避免仅凭总BD-rate猜测收益来源。
R4-3的候选补查来自实际代码的选择顺序，不是根据某条序列拟合新常数。
以下公式在新实验编码前冻结；未来R3的B/RA结果只影响是否继续投入，不悄悄改写本版本。

## 3. 沿用的定义

对当前位置的因果邻域L/U/D/LL/UU取可用的非零最终量化幅值集合T，保留重复样本，n=|T|。
不存在的邻居不是样本；当前待编码幅值、原始残差和未来位置都不参与决策。

\[
p_C=\max(L,U),\quad P=\{0,p_C\}\cup T.
\]

候选去重；p=0和p=1映射等价，规范化为identity候选0。n<3统一使用Current。
现有邻域包含Current来源，因此规范化后的不同候选至多6个，不引入新幅值公式。

沿用实际remapping：

\[
f(a,p)=\begin{cases}
0&a=0\\1&a=p>0\\a+1&0<a<p\\a&a>p.
\end{cases}
\]

沿用 `syntaxCost` 的C_TS，包括原Rice长度/escape、TU内不可变Rice和dynamic range；不更换为log proxy或fractional模型。

\[
d_i(p)=C_{TS}(f(a_i,p_C))-C_{TS}(f(a_i,p)),
\]
\[
G(p)=\sum_i d_i(p),\quad B(p)=\max(0,\max_i d_i(p)),\quad H(p)=G(p)-B(p).
\]

这都是**语法代理成本**，不是实际CABAC bits。删除的是固定候选的一项贡献，不是重新训练的leave-one-out验证或置信区间。

R3-1先找G最大的候选p_R，沿用Current优先、identity优先、再小幅值的平局规则；
仅H(p_R)>0时采用，否则Current。令最终输出为p_3，定义active为p_3与p_C的remapping不等价。

## 4. R4-1：只保留R3已接受的identity分支

\[
p_I=\begin{cases}
0&active\land p_3\le1\\
p_C&其他.
\end{cases}
\]

计算完整R3 proposal与guard，然后只保留其中选择NoPred的情况。
**不是**把候选集合提前删成{Current,NoPred}重新搜索；那会改变winner和触发位置，混入另一个问题。

目的：用真实闭环编码检验“局部禁用prediction”本身能否解释大部分R3收益。
同一因果输入下，如果R3选择非identity幅值，本组回到Current；不会另找一个NoPred机会。

若本组接近/优于R3，只说明保留正幅值分支的增量不足；不能立即声称已降低计算复杂度。
本消融仍计算完整R3 winner，删掉搜索开销需要另一个等价证明或后续简化实验。

## 5. R4-2：只保留R3已接受的非identity幅值分支

\[
p_M=\begin{cases}
p_3&active\land p_3>1\\
p_C&其他.
\end{cases}
\]

同样先运行完整R3选择，再屏蔽identity分支；不是从候选集合移除0后重新找winner。
本组才检验“选一个不同于Current的正幅值”本身的净价值。

在**完全相同的因果输入**上，I与M的有效改动集合互斥，二者并集为R3的有效改动集合。
Current、I、M、R3构成“不启用/仅identity/仅幅值/两者”的分支消融矩阵。
但四次实际编码的q、TS选择和后续参考可以不同；**不要求I和M的BD收益相加等于R3**。
不要强行按坐标配对不同闭环的CG，也不要用原R3系数离线替代两个编码实验。

## 6. R4-3：只在R3回退时补查稳健候选

### 6.1 现有顺序留下的空缺

R3对G最大者做保护，并不保证它也是H最大者。
一个大幅值单样本可能提高总G，却让G−B失败；其他候选未必失败。
当前实现对此直接Current，不再检查其他候选。

### 6.2 冻结规则

1. 完整计算R3。如果R3已采用与Current不等价的p_3，**原样返回p_3**。
2. n<3仍Current。若R2 proposal已经与Current等价，也直接Current：此时其他候选G≤0，H≤G，不可能通过。
3. 只有R2提出替代但被R3保护拒绝时，对原有候选集合P计算H和G。
4. 在H>0的候选中，按 **H较大 → G较大 → identity优先 → 较小幅值** 的顺序选择。
5. 没有通过候选仍Current。**H=0仍拒绝，不减少支持数、不改变cost或Rice、不放宽阈值。**

令p_*为上述补查结果：

\[
p_{R4C}=\begin{cases}
p_3&active\\
p_*&\neg active\land n\ge3\land H(p_*)>0\\
p_C&其他.
\end{cases}
\]

与“一开始就最大化H”的全局重排不同，这个版本特意保留R3所有已接受选择，只研究回退路径的完整性。
H可以理解为保持候选固定、删除任一单个贡献（或不删）后的最差总优势；它仍会受邻居相关性和选择偏差影响。

### 6.3 用真实成本函数核对的非等价例子

取模板 `[L,U,D,LL,UU]=[2,1,10,1,1]`，Rice=1、dynamic range=15，Current=2：

| 候选p | G | 最大正贡献B | H | 作用 |
|---|---:|---:|---:|---|
| Current=2 | 0 | 0 | 0 | 参考 |
| 10 | 5 | 7 | −2 | R2总分winner，被R3拒绝 |
| NoPred=0 | 4 | 2 | **2** | 总分稍低，但通过同一个保护 |

R3输出2；R4-3输出0。新规则并不是在失败后“放宽保护”，而是找到原集合中本已合格的另一候选。
这只是代码结构的真实反例，**不是该系数真实CABAC收益或CTC改善证据**。

### 6.4 必须承认的风险

- 在固定因果输入上，它只会增加R3回退区域的启用机会，而非收紧保护；更多通过可能引入过拟合和新损失。
- 固定输入时保留p_3，不代表闭环编码会保留R3原本所有收益，因为前面一次rescue会改变后续q与邻域。
- R3可能偶然受益于直接回退；“搜索更完整”不等于真实RD更好。
- 实际rescue位置可能很少，或集中在最终不改变映射的位置，导致工程价值不足。
- 新开销只发生在拒绝路径，但这一路可能很多；最多对原有6个候选×5个样本评分，仍须测Decoder开销，不能仅凭整数运算称为免费。

## 7. 核心设计的范围限制

- 不加Y-only/U-off/V-off：R3的E结果未支持简单分量关闭能解决剩余退化，先保持一个清楚的YUV主版本。
- 不加CU-QP阈值：质量区间不是CU-QP；此前门控未稳定奏效。
- 不扫描“删除0/1/2项”、n阈值、邻居权重、衰减K；本轮保留同一个H>0和n>=3。
  v2的DR另测试一个预先固定的2:1方向相关性假设，不作权重比例扫描。
- 不立即增加CG历史组或跨TU状态：R3-3/4未通过扩展规则。I/M/C均属于逐系数局部因果预测，
  **不能冒充仅利用之前CG选择整CG的原始研究问题已经解决**。
- 不因为某序列最差就加序列、class、分辨率、POC特例；KristenAndSara是诊断案例，不是专用开关目标。
- 不同时添加“全局H最大化”与“仅回退补查”两套再挑赢家；本轮只测后者。

原始历史CG方向保留为后续独立问题：需要先有与真实分支贡献有关的历史可预测性证据，而不是继续累积相同的平均score。

## 8. 最小机制统计

实施时只在最终Writer聚合，不污染RDOQ或Decoder状态，不默认逐系数输出。
按component/W/H/CG数/CG位置/CU-QP/intra-inter分组；sequence/config/名义QP来自任务元数据，不把分辨率当TU尺寸。

至少记录：

- 最终TSRC TU/CG、系数/非零数、regular与bypass数；CBF0/TSRC-off不进入的范围明确。
- 在本工具自己的最终q邻域上重算原R3决策：Current、identity、other三类；p=0/1合并。
- 原R3提出/接受/拒绝；各分支实际预测改变、实际remapping改变数量。
- I/M屏蔽的分支数；C进入补查、存在合格候选、最终rescue identity/other、实际mapped改变数量。
- n、G/B/H的固定分桶；rescue前R2 winner与rescue候选的G/H差，零/正/负分开。
- 单CG、两CG、多CG TU及所有真实W×H；不把“选择一次”当作“节省一次”。

可在最终系数上统计实际目标对应的proxy cost差、hit/under/over，按分支分层；这些只作条件机制诊断。
**当前q只能用于事后观察，不能回流到当前位置predictor选择。** 主收益仍为闭环总码流BD-rate。
任何固定q收益、原R3在新q上的虚拟分支分类，都不是R3真实编码结果的CG级因果对照。

R3正在跑的程序不要为了增加统计而替换；先读取已有日志。若日志字段不够，另起明确命名的短帧诊断，保持原长跑指纹不变。
无日志时仍可分析完整RD表，但不能判定分支启用比例、TU尺寸归因或Decoder复杂度已验证。

## 9. 编码与评价计划

### Phase 0：未来实施与正确性

- 独立版本/构建目录，不覆盖 `build/ts-r3/`，不修改进行中的任务。
- Current/OFF对保留anchor、旧R3各模式对保留R3程序做bit-exact回归；统计开/关bit-exact。
- I/M的固定输入分支互斥与并集性质；C保持R3已接受预测、rescue必满足原H>0的单元断言。
- 手工上述反例、无合格候选、n不足、identity等价、边界/矩形/全部实际支持TS尺寸、Rice、BDPCM/bypass验证。
- RDOQ/Writer/Reader同一预测实现，未来q污染和多遍调用一致性；不引入任何新的状态更新时机。
- 少量合成及真实四QP短帧验证实际mapped改变、解码hash、有效模式banner；不按短帧RD挑序列。
- 若C找不到真实有效rescue，先记录覆盖不足并暂停C的扩展，不修改规则来凑活动。I/M也必须检查被保留分支并非空集。

### Phase 1：三组LB CE

完整C4+E3、QP22/27/32/37、LBeu半帧，I/M/C各28项，共84项。
复用已保存Current与R3-1完整结果，前提是远端源码/配置/输入/帧数可比性得到确认；不明时注明条件性，不偷偷重跑整套anchor。
与当前批量流程保持一致：不输出重建、共享任务池、不设置组间等待屏障、逐组结束即写表、支持续跑。
六组模式现已加入原脚本；具体命令见实现文档，正式长跑仍由用户决定。

### 主口径与对照

- 正式主BD对Current，分量PCHIP后6:1:1，CE七序列等权、BCE十二序列等权。
- 同时给直接相对R3-1的重新积分BD；不把两个对Current的BD相减当作直接收益。
- I/M与R3的差别是闭环分支消融，不能相加。所有组缺点不自动补anchor。
- 逐序列/分量/C/E、中位数、std、分位数、去PartyScene及全部leave-one-out、序列配对bootstrap、cubic敏感性。
- 高/中/低质量区间仍用原四点PCHIP；与R3比较时取Current/R3/新方法三方共同外部区间，Current QP32/27为内部切点。
- 独立报告候选覆盖、实际mapped改变与复杂度；不要让数百万系数使极小总收益看似独立显著。
- CE已多轮参与设计，是开发集合。R3后续B/RA数据已在观察计划中，也不宣称全部是未见验证数据。

### Phase 2：不自动三组铺开B/RA

I/M主要用于解释机制，不自动与C一起进入长跑。

- C要继续扩展：完整CE对Current平均<0、中位数<=0、去Party后<=0；
  直接对R3-1平均<0、配对中位数<=0；共同低质量区间不劣于R3-1且高质量仍对Current有收益；
  有实际rescue/remap覆盖及可核对来源，开销可接受。
- I/M若明显改善且通过同样的RD/稳定性要求，可以作为删去无效分支的候选；
  若仅接近R3，则只作机制结论，不在没有实际复杂度减少时以“更简单”为由宣称工程升级。
- 同轮最多选一个R4候选扩展，按完整CE直接对R3-1的均值排序；精确平局优先实测Decoder开销更低者，再按较小编号。
  这是预先资源规则，不代表统计上证明winner；CI跨0须保留不确定性。
- 是否进入RA/B需结合届时R3结果单独决定。默认沿用先RA CD32项、RA均值<=0再LB B20项，
  实际RA帧数必须匹配anchor。不因新增R4而取消或重置R3正在进行的验证。
- 原三组核心计划最多84+32+20=136个新增正式任务，不含短帧。
  v2若六组全执行则CE共168项，加同一份最多52项扩展，上限220项；不是136+220。
  两份设计合计最多一个R4候选扩展，不分别挑赢家；这些都是计划，不是已授权/已启动任务。

工程目标仍为真实BCE −0.05/−0.08/−0.10%。不把CE小幅改善直接等同达标，也不套用旧方法B数据。

## 10. 预先解释结果与证伪

| 结果 | 可作的结论 | 下一步 |
|---|---|---|
| I接近/优于R3，M明显弱 | R3的主要闭环价值可能来自局部选择NoPred | 再研究真正降低搜索开销的二候选选择；不是直接删候选等价替换 |
| M接近/优于R3，I明显弱 | 非identity幅值预测是主要保留方向 | 不继续默认混入NoPred能获益 |
| I/M各自一般，R3明显更好 | 两分支可能有互补或闭环交互 | 保留联合工具；不能据表格证明具体CG的互补选择准确 |
| I/M都差 | 单独分支不足，R3联合行为重要或效果不稳定 | 不按序列拼接I/M赢家 |
| C有足够rescue却退化 | 代理H合格不是实际RD改善保证 | 否定这版补查，不扫描更多H阈值 |
| C几乎无有效mapped改变 | 该结构空缺实际覆盖不足 | 降低优先级，不因代数例子存在就坚持长跑 |
| C只靠PartyScene补强平均，其他内容/区间退化 | 仍缺稳定增量 | 不扩展、不为退化序列关工具 |
| R3后续B/RA失效 | 现有局部优势推广不足 | 降级整个方向，重新评估R4资源，不悄悄替换已完成R3结果 |

失败也要完整报告。NoPred分支占比高低、rescue出现频率、代数H大小都不能替代真实RD结果。
本设计不预设三个新组优于R3，亦不将“补查更完整”当成“统计更准确”的证明。

## 11. 设计阶段完成的工作（历史记录）

以下描述编码实现前的设计交付；六组现已实现，当前宏/测试见实现与验证文档。

- 仅新增设计文档、台账记录、结果收件目录，以及独立代数检查程序 `scripts/ts_r4_design_check.cpp`。
- 检查程序直接复用源码R3的remapping、syntaxCost和guard，遍历五位置幅值0..15、Rice=1/2/4、range=15，
  共3,145,728个模板/Rice组合；固定输入I/M分解及C保留R3已接受预测、rescue满足H>0检查通过。
  出现2520个rescue组合，证明C不是R3的等价重写；这些是人工枚举计数，不是视频活动率或收益估计。
- 没有修改codec、CMake、TypeDef.h、batch或任何已保存结果，没有编译/覆盖Encoder或Decoder，也没有启动编码。
- 未来可考虑 `JVET_BJUT_TS_R4_MODE=0..6`（v2增加4/5/6），与旧fixed/revision1/R2/R3互斥、宏优先、显式参数可覆盖；
  **目前该宏和模式均不存在，不能按此直接启用。**
- 收件入口（现按宏编号命名）：`experiments/ts_predictor_r4/r4_<R4_MODE>_<方法名>/LB_CE/`，只有README，不生成空白XLSM冒充结果。

代数检查可单独编译，不是codec实验程序：

```bash
r4_check_dir=$(mktemp -d /tmp/ts-r4-design-XXXXXX)
g++ -O2 -std=c++17 -Isource/Lib/CommonLib scripts/ts_r4_design_check.cpp \
  -o "$r4_check_dir/ts_r4_design_check"
"$r4_check_dir/ts_r4_design_check"
```

正式实验命令需要后续实施、正确性验证及用户决定运行后再提供。
